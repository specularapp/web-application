import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Termos de uso",
  description: "Condições de uso da plataforma Specular",
  path: "/termos",
  origin: "site",
});

export default function TermsPage() {
  return null;
}
