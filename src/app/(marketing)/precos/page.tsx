import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Preços",
  description: "Planos para freelancers e agências, sem surpresa e sem fidelidade",
  path: "/precos",
  origin: "site",
});

export default function PricingPage() {
  return null;
}
