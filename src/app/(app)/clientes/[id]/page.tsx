import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Cliente",
  description: "Dados, oportunidades, orçamentos, contratos e cobranças do cliente",
});

export default function ClientPage() {
  return null;
}
