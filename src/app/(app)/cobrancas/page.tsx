import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Cobranças",
  description: "Cobranças enviadas, pagas e em atraso",
  path: "/cobrancas",
});

export default function BillingPage() {
  return null;
}
