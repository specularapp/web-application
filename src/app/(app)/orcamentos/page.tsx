import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Orçamentos",
  description: "Crie e acompanhe orçamentos enviados aos clientes",
  path: "/orcamentos",
});

export default function QuotesPage() {
  return null;
}
