import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Perfil",
  description: "Seus dados pessoais e preferências",
  path: "/configuracoes",
});

export default function ProfilePage() {
  return null;
}
