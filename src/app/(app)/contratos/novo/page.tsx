import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Novo contrato",
  description: "Gere um contrato a partir de um orçamento aprovado",
  path: "/contratos/novo",
});

export default function NewContractPage() {
  return null;
}
