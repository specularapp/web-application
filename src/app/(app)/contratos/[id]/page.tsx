import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Contrato",
  description: "Detalhes, assinatura e status do contrato",
});

export default function ContractPage() {
  return null;
}
