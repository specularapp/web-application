import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Projeto",
  description: "Detalhes, entregas e mídia do projeto",
});

export default function ProjectPage() {
  return null;
}
