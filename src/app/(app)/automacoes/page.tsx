import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Automações",
  description: "Lembretes, cobranças automáticas e integrações",
  path: "/automacoes",
});

export default function AutomationsPage() {
  return null;
}
