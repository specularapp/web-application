import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Financeiro",
  description: "Entradas, saídas, custos e saúde financeira",
  path: "/financeiro",
});

export default function FinancePage() {
  return null;
}
