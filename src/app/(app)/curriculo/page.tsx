import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Currículo",
  description: "Currículo sempre atualizado a partir dos seus projetos",
  path: "/curriculo",
});

export default function ResumePage() {
  return null;
}
