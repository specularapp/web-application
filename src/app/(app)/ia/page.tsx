import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Assistente de IA",
  description: "Consulte e execute ações no sistema com ajuda da IA",
  path: "/ia",
});

export default function AssistantPage() {
  return null;
}
