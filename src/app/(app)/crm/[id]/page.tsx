import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Oportunidade",
  description: "Detalhes e histórico da oportunidade",
});

export default function OpportunityPage() {
  return null;
}
