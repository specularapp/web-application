import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Proposta comercial",
  description: "Proposta enviada para sua avaliação",
  noIndex: true,
});

export default function PublicQuotePage() {
  return null;
}
