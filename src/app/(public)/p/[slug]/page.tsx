import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Portfólio",
  description: "Portfólio de projetos publicado com Specular",
});

export default function PublicPortfolioPage() {
  return null;
}
