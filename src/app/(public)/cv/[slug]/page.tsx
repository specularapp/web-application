import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Currículo",
  description: "Currículo publicado com Specular",
});

export default function PublicResumePage() {
  return null;
}
