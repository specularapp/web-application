import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "Pagamento",
  description: "Cobrança enviada para pagamento",
  noIndex: true,
});

export default function PublicInvoicePage() {
  return null;
}
