# Estrutura e stack

## Stack

| Tecnologia | Papel |
| --- | --- |
| Next.js 16 (App Router) + TypeScript | Front e camada de servidor (Server Components, Server Actions, Route Handlers) |
| Supabase | Autenticação, banco Postgres com RLS, storage |
| Redis (ioredis) | Cache de leituras repetidas e rate limit |
| Resend | E-mail transacional e de automação |
| Stripe | Pagamentos e recebimentos (o doc original dizia "strapi", que é um CMS; a integração é Stripe) |
| n8n | Automações e integrações externas via webhooks |
| Emotion (`@emotion/styled`) | Styled components, consumindo os tokens CSS |
| cmdk | Paleta de comandos |
| Phosphor Icons | Ícones |
| zod | Validação de entrada no servidor |
| ESLint + jsx-a11y | Qualidade e acessibilidade |

## Organização de pastas

```
src/
  app/                  rotas (apenas roteamento, sem lógica de domínio)
    previa/             prévias de front, só em homologação (fora dela a página é 404 e o proxy fecha)
    (marketing)/        site público: home, preços, termos, privacidade
    (auth)/             login, cadastro, recuperação de senha
    (app)/              área autenticada, protegida pelo proxy
    api/v1/             mesma regra das actions exposta ao aplicativo, por Bearer token
    (public)/           páginas compartilháveis com clientes: portfólio, currículo, orçamento, contrato, cobrança
    api/                health e webhooks (stripe, n8n, resend)
    auth/callback/      troca de código do Supabase
    componentes/        vitrine do design system, pública e noindex
  components/
    ui/                 primitivos reutilizáveis (Button, Input, Card)
    layout/             shell da aplicação (Sidebar, Topbar)
    providers/          providers globais (Emotion, tema)
  features/<dominio>/   regra de negócio por domínio: actions, queries, schemas e componentes específicos
  hooks/                hooks compartilhados
  lib/                  clientes das integrações, metadata, env, segurança, utilitários
  styles/               reset.css, tokens.css, globals.css
  types/                tipos globais e tipos gerados do banco
  docs/                 regras, objetivo e estrutura
  proxy.ts              headers, sessão, MFA e proteção de rotas
db/
  README.md             convenções de banco e migrações
  migrations/<dominio>/ fonte da verdade das migrações, por domínio
  supabase/             projeto do Supabase CLI (config, seed; migrations é gerada)
```

## Domínios em `features`

`auth`, `organizations`, `onboarding`, `dashboard`, `crm`, `clients`, `quotes`, `contracts`, `billing`, `projects`, `tasks`, `portfolio`, `resume`, `finance`, `automations`, `ai`, `gamification`, `settings`.

## Convenções

- Rotas em português, código em inglês.
- Páginas só compõem componentes de `features` e `components`.
- Estilo sempre com tokens de `styles/tokens.css`. Primitivos sem interação (Text, Stack, Container, Table, Label, Field, Logo...) são Server Components com CSS Modules (`*.module.css` ao lado do componente, variantes por `data-*`), zero JS no cliente. Componentes interativos (Button com loading, Dialog, Switch, Toast...) usam Emotion e `"use client"`. Nada de valores soltos.
- Todo componente novo entra na vitrine `/componentes` com nome e exemplo, conferido nos dois temas.
- Tela grande também ganha prévia em `/previa/*`, que é a vitrine no formato de verdade: tela cheia em vez de caixa, para ajuste visual sem sessão e sem dado no banco. `/previa/primeiros-passos` abre qualquer uma das três etapas por `?etapa=time|membros|plano`, e na etapa do plano escolher um plano pago abre a etapa de pagamento com o espaço do formulário do Stripe reservado. `/previa/plano` mostra `/configuracoes/plano` com assinatura em teste, cartão e faturas de exemplo. `/previa/painel` mostra o painel inteiro, menu e blocos, com os dados de exemplo do menu. As rotas existem só em homologação: o proxy libera o prefixo `/previa/` por `isHomologation()` e a própria página devolve 404 fora dela. Os estados de exemplo vivem num lugar só, em `features/billing/preview.ts`, compartilhados com a vitrine.
- `OnboardingFlow` separa `demo` (não toca o servidor) de `boxed` (caixa dentro da página, o formato da vitrine): a prévia usa só `demo`, então ela aparece como a camada de verdade.
- Identificador que a pessoa vê é sempre o mesmo formato, em toda a aplicação (decisão de 2026-09-07): prefixo de três letras do domínio, ano e sequência de quatro dígitos, como `ORC-2026-0042`, por `formatReference` em `lib/utils/reference.ts` (`ORC` orçamento, `CTR` contrato, `COB` cobrança, `PRJ` projeto). A sequência reinicia a cada ano e cresce além de quatro dígitos se precisar; o id interno continua sendo o do banco. Nasceu no bloco de último orçamento, quando o usuário pediu um número curto e objetivo em vez de um código longo.
- Metadados via `createMetadata` em toda página.
- Dados via Server Components e Server Actions. Cache em Redis onde houver leitura repetida.
- Webhooks verificam assinatura. Toda entrada validada com zod no servidor.

## Fluxo de dados

1. Página (Server Component) chama `queries` da feature.
2. `queries` acessam Supabase pelo cliente de servidor, com cache em Redis quando fizer sentido.
3. Mutações passam por `actions` (Server Actions) com validação zod e revalidação por tag.
4. Eventos externos entram por `app/api/webhooks/*`, são verificados e despachados para a feature ou para o n8n.

## Design

### Cores

Paleta de cores do sistema Apple (Human Interface Guidelines, System colors), em `src/styles/tokens.css`:

- Primitivos `--sys-*`: red, orange, yellow, green, mint, teal, cyan, blue, indigo, purple, pink, brown e gray 1 a 6.
- Fundo do tema claro é `#fbfbfc`, e não branco puro (decisão de 2026-09-02): branco seco endurece a tela e some com a borda dos cartões, que são brancos de verdade (`--color-bg-tertiary`). O escuro segue em preto puro.
- Semânticos `--color-*` seguindo os nomes da Apple: label (4 níveis), placeholder, link, separator, bg (system e grouped, 3 níveis cada) e fill (4 níveis).
- Papéis do produto: `--color-brand` (preto no claro, branco no escuro, igual ao logo), `--color-accent` (system blue), success, warning, danger, info e focus.
- Claro e escuro com `light-dark()`, forçados com `data-theme="light"` ou `data-theme="dark"` no `html`. **Escuro é o padrão do produto**: sem cookie de preferência, o layout raiz põe `data-theme="dark"` em vez de seguir o sistema (decisão de 2026-09-01). Todo componente continua obrigado a funcionar nos dois temas, porque o claro segue disponível por preferência.

### Tipografia

- Inter (`--font-sans`) como principal, Geist Mono (`--font-mono`) só para código, Playfair Display (`--font-serif`) para títulos editoriais. Todas via `next/font`.
- Número é sempre Inter: `numeric` no `Text` liga `tabular-nums` e força `--font-body`, mesmo sobre `font="code"`. Nada de trocar de família para alinhar algarismo.
- Use `--font-body`, `--font-code` e `--font-display`, que já incluem os fallbacks.
- Escala de tipos da Apple: caption 2 e 1, footnote, subheadline, callout, body, headline, title 3 a 1 e large title.
- Tracking sai do tamanho, não do gosto: `--tracking-tightest` (-0.06em) de 22px para cima, `--tracking-tighter` (-0.04em) entre 16px e 20px, `--tracking-tight` (-0.02em) até 15px. Texto grande precisa de mais aperto que texto pequeno.

- Uma família só desde 2026-09-04: `--font-code` e `--font-display` apontam para a Inter, e Geist Mono e Playfair deixaram de ser carregadas. Os tokens ficam porque dizem o papel do texto; trocar a fonte de um papel volta a ser um lugar só.

### Controles

- Altura e respiro em `--control-height-*` e `--control-padding-*`, nos tamanhos `sm`, `md` e `lg`.
- `--touch-target` garante 44px em ponteiro grosso. O helper `controlMetrics` de `src/components/ui/styles.ts` aplica os três de uma vez.
- Cor de botão e de controle sai de variáveis locais (`--button-background`, `--button-foreground`, `--button-border`), definidas por `data-variant` e sobrescritas por prop quando precisar de cor sob medida.
- Botão cobre a família inteira por prop: texto puro, `iconStart`, `iconEnd`, `iconOnly` (quadrado, exposto como `IconButton` com `label` obrigatório) e `locked` com `plan`, que anexa a etiqueta do plano sem tirar o clique, para o clique levar ao upgrade.
- `loading` é o estado de espera de toda ação: o `Spinner` toma o lugar do `iconStart`, o botão se desabilita sozinho e ganha `aria-busy`, com opacidade 0.75 e cursor `progress` para não se confundir com o desabilitado (0.4). O spinner puxa `currentColor` e `--diameter: 1.2em` pela regra do próprio botão, então acompanha variante e tamanho; o rótulo do `Spinner` fica vazio de propósito, porque quem anuncia é o texto do botão, que troca para o gerúndio ("Entrando", "Salvando"). Nos formulários com `useActionState` vem de `pending`; nos botões de provedor OAuth, que são um `<form>` por botão sem estado próprio, vem do `useFormStatus` dentro de um subcomponente.

### Campos de formulário

- Fonte com piso de 16px por `fieldMetrics`. Abaixo disso o iOS dá zoom ao focar, e a escala de tipos tem tamanhos de 13 e 15px, então o piso não é opcional.
- Rótulo, caixa do campo e dica compartilham a mesma borda esquerda, sem recuo óptico. O rótulo recuado até o texto digitado foi testado e descartado: parece deslocado.
- Campo de texto não tem indicador de foco, por decisão de produto. É desvio consciente de `rules.md`, que exige foco visível: quem navega por teclado perde a referência de onde está. Botão e link mantêm o anel.
- Máscara sai da prop `mask` do `Input`, com os padrões em `src/lib/masks.ts`: `cpf`, `cnpj`, `document` (troca de CPF para CNPJ ao passar de 11 dígitos), `phone` (fixo ou celular pelo comprimento), `cep`, `date`, `currency`, `percent` e `integer`. Nunca escrever regex de máscara solta em feature.
- Máscara numérica preenche a partir do centavo: o valor bruto é a sequência de dígitos e a formatação divide por 100 com `Intl.NumberFormat` em pt-BR. Digitar 1, 2, 3 dá R$ 0,01, R$ 0,12, R$ 1,23. É por isso que dinheiro vai para o banco em centavos inteiros: o campo já entrega assim.
- Máscara de tamanho variável (telefone, documento) usa `NumberFormatBase` com `format` por função, nunca `PatternFormat` com padrão trocado por estado. `PatternFormat` corta o dígito que passa do número de marcadores do padrão atual, então com o padrão de 10 dígitos o 11º nunca chega e celular fica impossível de digitar.
- O anel de foco some, mas o estado de erro fica: borda vermelha no `FieldShell`.
- Unidade fica fora do valor: `R$` e `%` são um `FieldAffix` em semibold ao lado do controle, e o campo guarda só o número. Texto dentro de `input` não aceita peso parcial, então o afixo tem que ser elemento próprio.
- Data é seleção, não digitação: `DatePicker` sobre o `react-day-picker`, com mês e ano em lista. O popover vai por portal para o `body`, porque dentro do campo ele ficaria atrás do campo seguinte, pintado depois na ordem do DOM. Posição por `fixed`, abaixo quando cabe e acima quando não cabe. A máscara `date` continua para quem precisa digitar.
- `FieldShell` é o invólucro comum de todo campo: borda, raio por tamanho, estado inválido e desabilitado. Fundo transparente, o mesmo do botão `outline`, para campo e botão lerem como a mesma família sobre qualquer superfície; no escuro o fundo elevado destoava do botão ao lado. `Input` e `DatePicker` só põem o controle dentro. O canto é `corner-shape: squircle` nativo, declarado direto, e fica fora do recorte do fallback por conter o controle e os afixos.
- No mobile (abaixo de 48rem) o calendário vira **bottom sheet**: fixo na base, largura total, alça no topo, fundo em `--color-scrim`, página travada atrás, células maiores. O popover flutuante depende de medir o gatilho e reagir a rolagem e teclado virtual, o que no celular vira salto e recorte. `useMediaQuery` em `src/hooks` decide, com `useSyncExternalStore` para não divergir na hidratação.
- A legenda do calendário (mês e ano) é nossa, via `components.MonthCaption` do `react-day-picker` com `useDayPicker().goToMonth`. O `captionLayout="dropdown"` da lib usa `<select>` nativo, e a lista de um select é desenhada pelo sistema: não aceita fonte, raio, sombra nem cor. O `CaptionDropdown` é um listbox de verdade (`role="listbox"`, `aria-activedescendant`, setas, Home, End, Enter, Escape), com a opção atual marcada e rolada para a vista ao abrir.
- Esse listbox é o `Listbox` de `components/ui/listbox/`, genérico em `string | number`, com `placement` (`below` ou `above`, que vira dropup e troca o caret) e `prefix` (rótulo curto antes do valor, como "Mostrar 30"). Saiu do calendário no segundo uso, a `Pagination`.
- `actions` põe itens que executam em vez de escolher (remover, cancelar) no fim da lista, separados por um divisor, com `tone="danger"` quando destroem algo. Ação e opção dividem a mesma navegação, e é por isso que o item ativo passou a ser índice e não valor: seta e Enter precisam alcançar um item que não é um valor possível do campo. Substitui o botão solto ao lado do campo, que roubava espaço da linha e precisava de um lugar reservado para não desalinhar.
- `placement="auto"` decide o lado ao abrir, comparando o espaço abaixo do gatilho com a altura estimada da lista (número de opções pela altura fixa da opção, com teto). É estimativa e não medição porque medir só depois de montar faria a lista aparecer embaixo e pular para cima no mesmo quadro.
- `Select` é esse mesmo `Listbox` vestido de campo, não outro componente: o gatilho recebe altura, recuo, raio e fonte do tamanho de controle pelas variáveis locais (`--listbox-trigger-height`, `--listbox-trigger-padding`, `--listbox-trigger-radius`, `--listbox-trigger-font-size`, `--listbox-trigger-border`), com piso de 16px na fonte pelo mesmo motivo do `Input`. O valor sai num `input type="hidden"`, então funciona dentro de `form` sem JS de cola; `placeholder` aparece enquanto ninguém escolheu e o `id` vai para o gatilho, para o `Label` do `Field` apontar para ele. Não usa `select` nativo porque a lista do nativo é desenhada pelo sistema e não aceita fonte, raio nem cor.
- Largura de componente Emotion nunca sai de classe de CSS Module: a folha do Emotion é injetada em runtime, depois da do módulo, e com a mesma especificidade ela vence. Quem precisa apertar um `Select` numa linha (a lista de membros) põe o componente dentro de um `span` próprio e dimensiona o `span`.
- O gatilho do `Listbox` é estilizado por variáveis locais (`--listbox-trigger-height`, `--listbox-trigger-radius`, `--listbox-trigger-background`, `--listbox-trigger-background-hover`), com o visual do calendário como padrão. Quem o embute define as variáveis, sem `className` de fora.
- Ids de opção saem do índice, não do valor: `aria-activedescendant` é um IDREF e valor com espaço quebraria.
- As opções ficam em grid com 2px de respiro (`--space-half`), para o fundo da ativa e da selecionada não colarem uma na outra.
- Barra de rolagem interna é sempre `thinScrollbar` de `src/components/ui/styles.ts`: 6px, trilho transparente, polegar em `--color-fill-quaternary` que escurece no hover. Vale para listbox, tabela e qualquer painel que rola dentro de si; a rolagem da página fica com a barra do sistema.
- Rolar item para a vista dentro de um painel é `scrollTop` no próprio painel, nunca `scrollIntoView`: esse rola todos os ancestrais, e num popover fixo isso arrasta a página inteira.
- O calendário é todo neutro: hoje leva anel fino em `--color-separator`, selecionado leva `--color-brand`, foco leva `--color-label`. O acento azul fica de fora dessa parte por decisão de produto.
- A bolha de erro fica **sempre acima** do campo: para baixo ela cairia atrás do campo seguinte, que é pintado depois na ordem do DOM. Para cima, ela cobre o campo anterior, que já foi pintado.
- `Tooltip`: Geist Mono em footnote (13px) com peso medium, respiro de 8 por 12, raio `--radius-md` e canto nativo. Um só `--bubble-radius` alimenta o raio da bolha, o recuo da seta e o alinhamento, então mudar o raio não desalinha nada.
- A seta do `Tooltip` cai no centro do gatilho por geometria, não por medição: a bolha alinhada ao fim tem a borda a `raio + metade da seta` do centro do gatilho, e a seta fica a `raio` da borda da bolha. Os dois números se cancelam e o centro coincide em qualquer largura de gatilho.
- Preenchimento automático não pinta o campo: o `reset.css` cobre o fundo do navegador com `box-shadow` inset em `--field-background`, que cada campo declara com a própria superfície.
- O controle vive dentro de um `<span>` que carrega borda, raio e squircle, porque `input` é elemento substituído: não aceita filho, nem afixo, nem ícone dentro.
- `Checkbox` segue a mesma ideia: o `input type="checkbox"` nativo fica invisível por cima (opacidade zero, cobrindo o rótulo inteiro), e o quadrado desenhado é um `<span>` de 20px em `display: grid` com o ícone centrado por `place-items`, borda CSS de 1,5px e `corner-shape: squircle` nativo no raio `sm`. O raio `xs` foi testado e parecia quadrado: a superelipse é mais reta que o arco de círculo no mesmo raio, e em 20px isso some. Estado sai de `:has(input:checked)`, `:has(input:indeterminate)`, `:has(input:focus-visible)` e `:has(input:disabled)` no invólucro, que troca variáveis locais; nenhum JS além de escrever `indeterminate`, que só existe como propriedade do DOM. Marcado e indeterminado pintam `--color-brand` com o traço em `--color-bg`, o mesmo par do botão primário. Com `children` o invólucro é `label`; sem, é `span` e quem usa passa `aria-label` ou associa um `Label`.
- `Switch` é o mesmo esqueleto com `input type="checkbox" role="switch"`. Trilho em pílula (`--radius-full`, que por regra não declara canto) de 28 por 64 com 2px de folga, polegar largo de 36 por 24 que desliza por `translate` calculado a partir das próprias variáveis (`largura - polegar - 2 folgas`), então mudar um tamanho não desalinha o outro. `sm` é 20 por 44 com polegar de 24. Ligado é `--color-success`, o verde do sistema, como no iOS; desligado é `--color-fill`. Polegar em `--color-on-accent` (branco nos dois temas, como o iOS) com `--shadow-sm`. `prefers-reduced-motion` desliga a transição do deslize.

### Etiquetas

- `Badge` é a etiqueta de situação, categoria e contagem. Server Component com CSS Module, sem JS.
- **Um matiz gera tudo.** Cada `tone` define só `--badge-hue`; o CSS deriva três cores dele e nenhuma variante escolhe cor própria:
  - `--badge-ink`: matiz misturado com `--color-label` em 70/30. No claro puxa para o preto, no escuro para o branco, então o mesmo cálculo dá contraste AA nos dois temas. Amarelo é o único que precisa de 60/40.
  - `--badge-tint`: matiz em 14% de alfa no claro e 18% no escuro, por `light-dark()` sobre `color-mix()`. Alfa, e não cor opaca, para a etiqueta assentar em qualquer superfície.
  - `--badge-line`: matiz em 36% e 44%, para a borda do contorno.
- Variantes só recombinam as três: `soft` (tinta sobre tint), `solid` (tinta como fundo e `--color-bg` como texto, o mesmo par de `--color-danger-solid` e `--color-on-danger`) e `outline` (tinta sobre transparente com a linha).
- Tons em dois grupos: papéis do produto (`neutral`, `brand`, `accent`, `success`, `warning`, `danger`, `info`) para situação com significado, e a paleta inteira (`red` a `brown`) para categoria em que a cor é arbitrária. `hue` por prop aceita uma cor escolhida pelo usuário, como etiqueta de CRM, sem sair do modelo.
- Tamanhos e raios saem juntos: `sm` 20 de altura e raio 8, `md` 24 e raio 8, `lg` 28 e raio 12. Canto por `squircle()` com `clip` nas variantes sem borda, para o fallback recortar o fundo; `shape="pill"` fica no `round` e não declara nada.
- Só ícone exige `label`, que vira texto para leitor de tela, no mesmo contrato do `IconButton`. O tipo obriga: ou `children`, ou `icon` com `label`.
- Ícone herda `currentColor` e o peso `bold` que casa com o texto semibold, pelo `matchIconWeight` de `components/ui/icons.ts`, compartilhado com o Button. Não existe variante com ponto colorido (dot), por decisão de produto: a cor do texto e do fundo já dizem a situação.
- Etiqueta com remoção (o × do chip) é outro componente, interativo, e não entra aqui.

### Avatar

- `Avatar` recebe `name` e, opcionalmente, `src` e `seed`. Sem imagem entra o rosto do DiceBear no estilo Adventurer (`@dicebear/core` e `@dicebear/adventurer`, por `avatar/shape.ts`, o único ponto que importa a lib), desenhado a partir do `seed`, com o e-mail como semente para o avatar não mudar quando o nome muda.
- **O rosto gerado vem de uma rota, e não embutido na página** (varredura de peso de 2026-09-08). O `Avatar` monta `<img src="/api/avatar/{token}.svg">` e a rota (`app/api/avatar/[token]/route.ts`) desenha e devolve com `cache-control` de um ano e `immutable`. Antes o SVG do Adventurer, uns 11 KB, ia inteiro na marcação e de novo, escapado, na carga do RSC: no painel eram 42 rostos à vista e mais uns 70 dentro de janelas fechadas, somando **1 MB dos 1,67 MB da página**. Pela rota a mesma pessoa em cinco lugares custa uma requisição, e na segunda visita custa zero. `<img>` cru, e não `next/image`: o otimizador não mexe em SVG sem `dangerouslyAllowSVG`, que rebaixaria a segurança sem ganhar nada; é desvio consciente da regra de imagens.
- **O endereço leva um token, e não a semente.** `avatarToken` deriva o token por duas passagens de hash (cerca de 64 bits) e é ele que vira a semente do desenho. A semente costuma ser o e-mail da pessoa, e e-mail em endereço de imagem vaza para registro de acesso, cache de borda, `Referer` e histórico do navegador, o que num produto que guarda dado de cliente não se faz. O token é determinístico e sozinho basta para redesenhar, então a rota não guarda mapa nenhum. Consequência aceita: como a semente do desenho mudou, os rostos gerados ficaram diferentes dos de antes, uma vez só; nenhum avatar é persistido, e daqui para frente cada pessoa tem sempre o mesmo. O matiz do fundo continua saindo da semente original, então a cor de cada pessoa não mudou.
- A rota é pública por construção (o `matcher` do proxy já pula `/api`) e não expõe dado: token não volta a ser e-mail. Ela devolve `default-src 'none'` e `nosniff` no próprio arquivo, porque SVG na mesma origem é vetor de script quando o conteúdo escorrega, e aqui o conteúdo é gerado pela lib a partir de um token que casa com `[a-z0-9]`. **Fica sem rate limit, ao contrário do que `rules.md` pede para rota pública**: cada resposta é `immutable` e some no cache da borda, o trabalho por requisição é de um milissegundo com cache em memória por token, e um limite por Redis somaria ida e volta a cada um dos rostos da primeira carga, que é justamente o que a mudança veio cortar. Se algum dia a rota aparecer em pico de origem, o limite entra aqui. Trocou o `avvvatars-react` em 2026-09-04, a pedido: rosto no lugar de forma abstrata; o estilo foi Lorelei até 2026-09-05, quando virou Adventurer, também a pedido. Iniciais continuam descartadas, por decisão de produto. O SVG é gerado em código, sem DOM, então sai igual no servidor e no cliente, sem folha injetada nem nonce (o `GooberNonce` saiu junto). O único `id` do SVG é reescrito com o hash da semente, porque a mesma máscara em vários avatares colidiria e o `randomizeIds` da lib usa Math.random, que discordaria na hidratação; o `<metadata>` sai para não pesar em cada avatar.
- O fundo do rosto é um matiz da paleta do sistema escolhido pela mesma semente (`hashString` de `lib/utils/hash.ts`, o mesmo da frase do dia), em pastel a 32% sobre o branco fixo (`--color-on-accent`), igual nos dois temas: o traço do Adventurer é escuro e sumiria sobre fundo escuro, e o pastel opaco é o que deixa um avatar cobrir o vizinho no grupo. O raio e o recorte continuam no invólucro, que já esconde o overflow por causa da foto, então círculo e squircle saem de um lugar só.
- Foto de perfil do Google e do GitHub entra sozinha: o gatilho `handle_new_user` copia do metadata no cadastro e `handle_user_identity` faz o mesmo quando a identidade é vinculada depois, sempre com `coalesce`, para nunca sobrescrever o que a pessoa definir no produto. Com imagem usa `next/image` com `fill` e `sizes` fixo por tamanho; domínio remoto entra em `remotePatterns` quando o upload existir. Server Component com CSS Module.
- Tamanhos na escala dos controles: `xs` 24, `sm` 36 (`--control-height-sm`), `md` 44, `lg` 52. Círculo por padrão. `shape="squircle"` usa raio em metade do lado, que é a escala do botão de ícone (`--icon-button-radius-*`; `xs` usa `--radius-md`), então avatar e `IconButton` do mesmo tamanho têm o mesmo contorno.
- Canto por `squircleAuto()` quando `shape="squircle"`: nativo onde há `corner-shape`, e no fallback o recorte por `clip-path` entra sozinho porque o avatar já tem `overflow: hidden` para a imagem. O anel do grupo é `box-shadow` no próprio elemento e não é recortado.
- `AvatarGroup` só sobrepõe: cada avatar seguinte entra 8px sobre o anterior, e todos ganham anel de 2px em `--color-bg` por `box-shadow`, que não ocupa layout e não é recortado pelo `overflow` do próprio avatar. Sem contador "+N" por enquanto.

### Toast

- Todo toast tem ícone, título, descrição e um botão de ação, sem botão de fechar. Sem `action` o botão vira "Entendi" e só fecha. Contrato e comportamento em `libs.md`.
- `Toast` em `components/ui/toast/` é só o visual, e por isso aparece estático na vitrine. Fila, timer e posição ficam no `ToastProvider` de `components/providers/toast-provider/`, montado no layout raiz dentro do `EmotionRegistry`. `useToast()` devolve `toast(options)` e `dismiss(id)`.
- Geometria concêntrica: raio `--radius-xl` (24) com padding 12 dá 12 de raio interno, então o botão de ação é `Button size="sm" radius="md"`. Canto nativo declarado direto, como o Tooltip; fora do recorte do fallback porque contém um botão com anel de foco.
- Animação só de `transform` e `opacity`, que o compositor resolve sem layout: entra com `--ease-spring` em `--duration-slow`, sai em `--duration-base`. A saída existe porque `dismiss` primeiro marca `open: false` e só remove 240ms depois. `prefers-reduced-motion` corta as duas.
- Timer por item, com pausa em hover e foco: o efeito começa um `setTimeout` com o tempo restante e, ao pausar, desconta o que passou. Erro (`danger`) não fecha sozinho; os outros fecham em 5s. Escape fecha o toast focado.
- Três visíveis no máximo; o quarto espera na fila e só começa a contar quando aparece. `role="alert"` para erro e atenção, `role="status"` para o resto.
- Canto superior direito no desktop, largura total no topo no mobile com `safe-area-inset-top`. A entrada desce de cima (`translateY` negativo), no sentido de onde o toast vem. Ícone centrado na altura do bloco de texto.

### Paginação

- `Pagination` é controlada: recebe `page`, `pageSize`, `total` e devolve `onPageChange` e `onPageSizeChange`. Quem chama decide se vai para a URL ou para o estado, e volta para a página 1 ao trocar a quantidade. Sem `onPageSizeChange`, o dropup nem aparece.
- Três partes: `Listbox` com `prefix="Mostrar"` e `placement="above"`, faixa "1 a 30 de 720" em `Text` numérico e secundário, e o grupo com página atual em negrito, total em secundário e dois `IconButton` fantasmas separados por `Separator` vertical. A página atual tem leitura completa ("Página 1 de 24") em texto oculto e `aria-live`.
- Geometria na escala do botão de ícone, raio em metade da altura: barra de 44 com raio 22 (`--icon-button-radius-md`) e padding 4; gatilho e grupo de 36 com raio 18 (`--icon-button-radius-sm`). 22 menos 4 dá 18, então o encaixe é concêntrico, e os `IconButton` `sm` de dentro, também com raio 18, casam com o grupo no hover.
- Barra e grupo declaram `corner-shape: squircle` direto, sem `data-squircle`, e ficam fora do recorte do fallback: contêm botões com anel de foco e um dropup que não podem ser cortados. A linha fina vem de `box-shadow` de 1px, que não ocupa layout e mantém a conta 36 em 44.
- Fundo da barra em `--color-fill-quaternary`, translúcido, para assentar em qualquer superfície; gatilho e grupo em `--color-bg-tertiary`, que fica mais claro que a barra nos dois temas.
- Larguras fixas para a barra não pulsar ao navegar: o indicador de página tem 5.5rem centrado (cabe "999 / 999" em algarismo tabular) e a faixa de itens tem no mínimo 12rem. Assim trocar de página ou de quantidade não move o grupo de botões.
- A barra nunca quebra linha (`flex-wrap: nowrap`). Abaixo de 48rem (`MOBILE_QUERY`, por media query em CSS, sem JS) o dropup e a faixa somem e fica só o grupo com página, anterior e próxima. No desktop aparece tudo.

### Barra de rolagem

Desenho único em `globals.css`, valendo em tudo que rola: polegar de 3px em `--color-fill-quaternary` que escurece no hover, com 2px de folga de cada lado e nas pontas (pedido de 2026-09-07: a barra não encosta em borda nenhuma do que rola; a folga sai de uma borda transparente no polegar com `background-clip: padding-box` e de `margin` no trilho, então a barra ocupa 7px de layout), trilho transparente e nenhuma seta nas pontas.

- As duas famílias não convivem. Assim que `scrollbar-width` ou `scrollbar-color` aparecem na regra, o Chrome descarta os pseudo-elementos `::-webkit-scrollbar` e cai na barra nativa, que no Windows vem grossa e com uma seta em cada ponta. Por isso as propriedades padrão ficam dentro de `@supports not selector(::-webkit-scrollbar)`, que só o Firefox atende, e o desenho vive nos pseudo-elementos.
- Pelo mesmo motivo, componente nenhum declara `scrollbar-width: thin` por conta própria: fazer isso desliga o desenho global naquele elemento. Quem quiser esconder a própria barra declara `scrollbar-width: none`, que é o caso do menu.


### Cantos

- Todo canto arredondado passa pelo sistema de cantos da casa, sem lib. `squircle("lg")` de `src/lib/corners.ts` emite `data-squircle` e `data-squircle-radius`; `squircleAuto()` emite só `data-squircle` e deixa o raio para o CSS; `squirclePx(n)` para raio fixo fora da escala. `border-radius` continua sempre no CSS: é o fallback e é o que o motor lê quando o raio não vem no atributo.
- Onde existe `corner-shape` (Chromium 139+), `globals.css` aplica `corner-shape: squircle` em `[data-squircle]` e acabou: fundo, borda, sombra, anel de foco e recorte de filhos saem do motor do navegador, sem JS. O motor detecta o suporte com `CSS.supports` e não liga nada.
- Sem `corner-shape` (Safari, Firefox), o motor em `src/lib/squircle/` recorta por `clip-path: path()` com a superelipse (suavização 0,6, a do iOS, pelo algoritmo de cantos suavizados do Figma em `path.ts`). Só recorta quem já recorta (`overflow: hidden` ou `clip`) ou quem pede com `{ clip: true }`. Tudo o mais fica no `round` do `border-radius`. Motivo: recorte cortaria anel de foco de botão e popover que sai do container, que foram exatamente os bugs do cornerKit.
- Elemento com borda nunca é recortado: a borda CSS segue o arco de círculo e o recorte seguiria a superelipse, então ela apareceria quebrada no canto. É por isso que nenhuma variante do fallback desenha borda; borda é sempre CSS.
- Motor (`engine.ts`): um `ResizeObserver` só para todos os elementos, escrita agrupada por `requestAnimationFrame`, cache de caminhos por tamanho e raio, `MutationObserver` para elemento que entra, sai ou troca de atributo, `WeakMap` para o que já foi aplicado. Escrever `clip-path` não muda layout, então não realimenta o observer: sem loop por construção. Nada de `<style>` injetado, então a CSP fica em paz. `SquircleProvider` no layout raiz só liga o motor uma vez.
- Componente client com raio dinâmico usa `useSquircle(raio, clip)` de `src/lib/squircle/use-squircle.ts`, que devolve um ref callback: emite os atributos e registra o elemento, com limpeza ao desmontar.
- Quem declara `corner-shape: squircle` direto no CSS, sem `data-squircle` (FieldShell, Tooltip, Listbox, Pagination, Checkbox, Toast), fica nativo no Chromium e redondo no fallback, de propósito: são elementos com anel de foco, popover ou conteúdo que não pode ser recortado.
- O cornerKit saiu em 2026-08-29 depois de uma sequência de bugs: `<style>` injetado bloqueado pela CSP, fundo repintado por SVG que atrasava e piscava, caixa de 20px do Checkbox virando invisível, borda semitransparente empilhando alfa, um observer por elemento. O nativo cobre o caso principal e o fallback nosso é pequeno, previsível e sem borda.
- Escala em passos de 4px para o encaixe concêntrico: xs 4, sm 8, md 12, lg 16, xl 20, 2xl 28. A regra da Apple é raio interno igual ao externo menos o padding, e nessa escala a conta sempre cai em outro token (20 menos 8 dá 12, 16 menos 4 dá 12).
- **Canto de dentro é sempre concêntrico.** Nada aninhado escolhe raio próprio: ele sai do raio do pai menos o padding que os separa. No CSS é `calc(var(--corner) - var(--pad))`, no TS é `concentric(raioDoPai, espacamento)` de `src/lib/corners.ts`. Quando os dois convivem, o mesmo número alimenta o `border-radius` e o `data-squircle-radius`, senão o fallback e o desenho do cornerKit discordam.
- Botão é o exemplo da casa: raio 20 no tamanho `md` e `padding-block` de 8 dão etiqueta interna de 12, que é outro token da escala. É por isso que a escala anda de 4 em 4.
- Botão só de ícone tem escala própria (`--icon-button-radius-*` e `iconButtonCornerRadius`), com o raio em exatamente metade do lado: 18 em 36, 22 em 44, 26 em 52. É o máximo que a superelipse aceita antes do clamp, e dá o formato de ícone do iOS. Passar disso não arredonda mais, só faz o CSS e o cornerKit divergirem.
- Nesse botão o lado é fixo (`width` e `height`, não `min-`), o padding é zero e o glifo é 45% da altura por `calc`, então a proporção do ícone é idêntica nos três tamanhos.

### Containers aninhados

- `Surface` é o container da casa. Empilhe à vontade: a profundidade sozinha define raio e padding, por seletor descendente no CSS Module. Ninguém passa nível nem faz conta no ponto de uso.

| nível | raio | padding | raio interno |
| --- | --- | --- | --- |
| 1 | 40 | 16 | 24 |
| 2 | 24 | 12 | 12 |
| 3 | 12 | 8 | 4 |
| 4 e além | 4 | 8 | 4 |

- A escada é concêntrica de verdade, não aproximação: em toda linha o raio interno é o externo menos o padding, e o resultado cai sempre num token existente. Só fecha assim por causa dos valores da escala (4, 8, 12, 20, 24, 32); mexer num deles quebra a corrente.
- O raio interno é o externo **menos** o padding, nunca mais. O arco do pai tem centro em (R, R); o filho recuado por P tem centro em (P + Ri, P + Ri). Para os centros baterem, Ri = R − P. Somar o padding empurra o centro do filho para dentro e desalinha: com pai 24 e padding 16, somar erra o centro em 32px.
- Por isso padding folgado se resolve **subindo o raio do pai**, não o do filho. O nível 1 usa `--radius-3xl` (40) justamente para caber 16 de padding e ainda sobrar 24 de raio interno.
- `Surface` não declara `data-squircle-radius`. No fallback o motor lê o `border-radius` computado. Assim o CSS continua sendo a fonte única do raio e a escada funciona em qualquer profundidade sem duplicar número em TS.
- Quem precisa de raio fixo e conhecido (Button, Table, Kbd) continua declarando o atributo, que tem prioridade sobre a leitura.

### Menu (Sidebar)

Uma composição só, duas apresentações. A ordem das partes não muda entre desktop e celular; o que muda é a moldura, e por isso o conteúdo mora num `SidebarPanel` com `variant`, enquanto o `Sidebar` decide a moldura por `useMediaQuery(MOBILE_QUERY)`. É troca de árvore, não de CSS, porque o rodapé tem conteúdo diferente nas duas formas.

De cima para baixo, seguindo a referência do usuário (a barra de projeto da Vercel): o time no lugar da marca, com logo de 24px, nome, etiqueta do plano e a seta dupla; campo de busca com a tecla de atalho; rotas em grupos separados por linha; e, colado no fim, o convite de ação, a meta e o perfil. A marca saiu do menu de propósito: quem identifica o contexto ali é o time, não o produto.

- O painel é avulso: sem fundo, borda ou raio em volta. **A página não abre recuo nenhum** (decisão de 2026-09-02): cada componente traz o seu por dentro, e o menu tem 8px internos no desktop e 12px na tela cheia do celular, na escala da referência. O valor sai de `--menu-padding`, declarado no painel, e a linha que sangra e a lista que rola leem dele, então os três nunca saem de sincronia. A coluna dele é trilha de grade, e não item de flex, porque trilha é limite duro: conteúdo nenhum cruza para cima do menu, nem quando o que está dentro passa da largura.
- Caixa sobrou só no convite, na meta e no perfil, todas com o preenchimento do botão secundário (`--color-fill-quaternary`), que sobre o preto fecha em #151517 e quase não aparece. Superfície opaca (`--color-bg-secondary`) foi testada e lida como cinza. O time não tem caixa nenhuma: fica solto no topo, separado por uma linha.
- Rótulo de rota em 75% do `--color-label` misturado ao fundo (`color-mix`), e não em token pronto: o secundário a 60% sumia no tema claro e o contraste cheio gritava. Hover e item em vigor sobem para o contraste cheio, e o item em vigor ainda ganha peso médio e preenchimento.
- Título de grupo não existe na tela: quem separa um grupo do outro é a linha, que sangra o recuo do menu pelos dois lados e corre de borda a borda. O nome do grupo continua para leitor de tela, em `role="group"` com `aria-label`. Linha só existe entre grupos de rota: nem abaixo da busca, nem antes do rodapé, e o vão em volta dela é o mesmo de uma opção para a outra, então a lista tem um ritmo só.
- Fio de 0,6px em todo o menu (`--menu-line`), mais fino que o 1px do resto da casa, e sempre em `--color-border` (12% no claro, 13% no escuro), nunca em `--color-separator`, que a 29% e 60% grita numa coluna estreita. Moldura some, sobra conteúdo. Ícone da lista em 18px e na cor do rótulo, herdada: ícone menor e mais claro que o texto quebrava a linha em dois pesos. Coluna de 16rem, na escala da referência.
- A busca parece campo e é botão, porque quem vai abrir é a paleta de comandos. Enquanto ela não existe, o botão fica de pé e mudo, com a tecla de atalho à direita num `Kbd`.
- Linha da lista em 36px, com 4px de recuo em cima e embaixo e 2px entre uma e outra. Em ponteiro grosso ela volta para 44px por `@media (pointer: coarse)`, que é a regra de alvo de toque: no celular a lista é o único jeito de navegar. A barra de rolagem do menu fica fora de vista (`scrollbar-width: none`), porque em coluna estreita ela come o respiro da direita.
- O cartão do rodapé é um aviso contextual, e não mais propaganda (decisão de 2026-09-07, sobre uma referência de lembrete de reunião): reunião marcada com cliente, entrega chegando, cobrança vencendo, tarefa no prazo. O modelo mora em `components/layout/alerts.ts` (`SidebarAlert`: tipo, título, linha de apoio, quando acontece, pessoas envolvidas e o atalho, tipado por rota ou endereço externo), com `describeAlert` escrevendo a linha de apoio ("9:00 a 9:30 no Zoom", "Amanhã, 9:00 a 9:30 no Zoom", "Vence hoje", "Vence em 2 dias", "Venceu ontem") e `pickAlert` escolhendo o mais urgente: o primeiro que ainda vai acontecer, ou o que passou há menos tempo. O `AppShell`, que é Server Component, escolhe e passa o aviso inteiro por prop, com a linha de apoio já escrita no servidor para não divergir na hidratação. No cartão: em cima as bolinhas de até três pessoas numa pílula com o fio da casa e "+N" para o resto, com o X que dispensa até a próxima carga na outra ponta; no meio o título e a linha de apoio; embaixo o atalho em `TextLink` sublinhado com a seta ("Entrar agora", "Ver projeto", "Ver cobrança"), em outra aba quando é endereço externo, e não botão, porque o cartão avisa, não vende. O mural de sugestões (`suggestions.ts`, de 2026-09-03) saiu junto com o sorteio por plano. **Os avisos não existem no banco**: vêm de `previewAlerts`, relativos a agora, e trocam só a origem quando reuniões, entregas e cobranças nascerem.
- Alerta de plano no formato da referência: identidade em cima (ícone tingido, "plano atual" e o nome do plano), frase curta no meio e botão cheio embaixo. O chip do ícone estica junto com o bloco de texto (`align-items: stretch` mais `aspect-ratio: 1`), então a altura dele é exatamente a das duas linhas, sem número chumbado que envelhece quando o tipo muda. Fundo é um gradiente do invisível para 16% de accent, e a borda 22%, sempre misturados com `transparent` e nunca com `--color-bg`: assim o cartão se apoia no fundo da página em vez de abrir um bloco branco no claro e preto no escuro. O ícone do botão vai sem `weight`, para o `Button` aplicar o peso da casa por `matchIconWeight` e a cor sair do próprio botão.
- Meta em uma linha só: "Meta", a barra ocupando o vão e o valor no fim, em caption 2. O nome inteiro ("Meta de faturamento") vive no `aria-label`, então quem usa leitor de tela não perde o contexto que a tela encurtou.
- Rota que abre no lugar: entrada com `items` (`NavFolder`) troca a lista inteira pelas páginas de dentro, com voltar no topo. Submenu aninhado em painel de 17.5rem empurraria tudo para a direita e sairia da vista.
- Na tela cheia o nav tem piso de altura, e com pasta aberta ele sobe para mais da metade da tela (`data-folder` no nav). A lista curta fica no alto e o perfil fica onde sempre esteve, embaixo, em vez de subir para encostar nela; o espaço em branco é o que separa os dois.
- A troca de pasta anima: a pilha do nav tem chave pela pasta, então cada troca remonta e entra deslizando na direção do movimento, da direita para dentro e da esquerda para fora. Abrir pasta também leva a rolagem ao topo de quem rola, que no desktop é o próprio nav e na tela cheia é a tela: lá a pasta costuma ser escolhida embaixo, e a lista curta que entrava no lugar ficava fora da vista.
- `nav.ts` é a fonte única do menu, e `href` é tipado por rota: página que não existe nem compila. Por isso tarefas, calendário e relatório da referência ficaram de fora até as páginas nascerem.
- O componente é apresentacional e recebe time, pessoa, convite e a lista de times por prop. Quem busca isso no banco é o `AppShell`, Server Component do grupo (app), que junta `getCurrentTeamState`, `getTeamOptions` e a cobrança e deriva o convite de plano.
- Meta de faturamento usa `compactMoney` de `lib/utils/format.ts`: R$ 30,5k, R$ 1,243M, sempre a partir de centavos, como todo dinheiro do produto.
- Com o menu aberto a barra encolhe até sobrar só o X e vira bolinha; ao fechar, expande de volta. O grupo de busca e sino recolhe pela trilha da grade, de `1fr` a `0fr`, que o navegador anima como número, porque largura `auto` não anima e trilha anima; o filho tem largura mínima zero e recorta. Vão e canto transitam junto, senão ficava um vazio entre o grupo recolhido e o X. O botão remonta a cada troca e o glifo que chega gira ao entrar, porque X e três linhas não se transformam um no outro. Na bolinha o X é círculo e ganha preenchimento (variante secundária): sozinho sobre o vidro, o traço fino sumia; fechado, volta a ser fantasma como os vizinhos.
- A barra flutuante não tem sombra: é vidro sobre a página, e a sombra a destacava como objeto em vez de deixá-la parte da tela.
- **A caixa da barra nunca muda de tamanho** (acerto de fluidez de 2026-09-08). Ela encolhia de verdade ao abrir o menu, animando trilha de grade (`1fr` para `0fr`), vão e raio: três propriedades de layout e pintura, na thread principal, embaixo de um borrão, no mesmo instante em que o React monta a tela cheia do menu. Enquanto a montagem segurava a thread, a animação perdia quadros e a troca parecia travar. Agora só andam `opacity` e `translate`, que o compositor resolve sozinho, então a barra fica lisa mesmo com a thread principal ocupada. Quem desenha são duas camadas irmãs que se cruzam, a pílula em `::before` e a bolinha em `::after`, e o grupo apaga encolhendo na direção do botão. A caixa segue larga quando recolhida, invisível: ela é `pointer-events: none` e só os filhos pegam toque. O deslocamento que centra a bolinha sai da própria largura da caixa, `calc(-100% + meia bolinha)`, porque porcentagem em `translate` mede o próprio elemento, então nada precisa ser medido em JavaScript. A tela cheia ganhou `will-change` junto, para a entrada e a saída dela também virarem trabalho de compositor.
- A barra flutuante declara `width: max-content`: elemento fixo com `left: 50%` encolhe até o espaço que sobra da metade para a direita, ou seja, meia tela, e o rótulo cortava por reticências mesmo com espaço de sobra na esquerda. O teto de largura impede o vazamento na tela estreita.
- A tela cheia do celular desbota na base num degradê para a cor do fundo, uma camada grudada no rodapé da tela que rola (`::after` em `sticky`), onde o conteúdo passa por trás da barra flutuante, em vez de ser cortado pela borda da tela. Era `mask-image` na tela inteira até 2026-09-07, e a máscara desbotava o fundo opaco junto: a página aparecia por baixo do menu.
- Celular: o painel não fica na tela. Uma barra flutuante no rodapé, ao centro, traz "Buscar" e o botão de abrir; aberto, o painel toma a tela inteira, sem a marca, e as ações da conta aparecem listadas em vez de escondidas atrás do botão de seta, porque no celular esconder opção atrás de camada custa um toque a mais e uma camada a mais.
- A seta dupla do time abre a janela de troca (`TeamSwitcher`, 2026-09-03), a primeira camada flutuante do menu. Opções da conta, busca e notificações vieram depois, e `DropdownMenu` saiu do stub em 2026-09-07, descrito abaixo.
- Prévia em `/previa/menu` (só em homologação), que é onde dá para ver a forma do celular, e a forma do desktop também entra na vitrine `/componentes`, em caixa alta.

### Movimento das camadas

Toda camada flutuante da casa entra e sai animando, e a receita mora em dois lugares só: `usePresence` em `hooks/use-presence.ts` e `layerMotion` em `components/ui/styles.ts`.

- **Curva por tipo de movimento** (2026-09-08). O que percorre distância usa `--ease-settle` (`cubic-bezier(0.22, 1, 0.36, 1)`): sai rápido e chega devagar, sem passar do ponto. É o que dá sensação de sistema em vez de site, porque a velocidade que a pessoa sente vem do começo do movimento, e não da duração total; a cauda longa é o que faz parecer macio. Por isso a duração pode ser generosa sem ficar lenta, e nasceu o `--duration-glide` (420ms) para o movimento principal. `--ease-standard` fica com troca de cor e de fundo, que não percorrem espaço, e `--ease-spring` segue no gênio das caixas coladas no gatilho, onde o pequeno exagero é o efeito. A regra veio de a barra do celular ter ficado rápida demais e seca quando as animações dela deixaram de animar layout.
- **Saída sempre mais curta que a entrada.** Quem fecha já decidiu, e segurar a camada no caminho de volta é o que faz interface parecer lenta. Entrada em `--duration-glide` ou `--duration-slow`, saída em `--duration-base`.
- **Peças que se movem juntas ganham durações diferentes, e não atrasos.** Na barra do celular são três (420 no deslocamento, 320 no vidro, 200 no grupo): partindo no mesmo instante, o grupo some primeiro, o vidro troca em seguida e o deslocamento ainda está pousando. Lê como coreografia sem `animation-delay` em lugar nenhum, que é uma coisa a menos para sair de sincronia.

- Fechar por toque fora é `useOutsideDismiss` em `hooks/use-outside-dismiss.ts`, o mesmo nas caixas coladas no gatilho (troca de time, notificações, opções da conta) e no `Dialog` sem escurecimento (decisão de 2026-09-05): apontar fora fecha a camada e engole o clique que vem em seguida, porque quem clica fora quer só fechar, e antes o botão que estava embaixo do ponteiro disparava junto. O ouvinte do clique entra na captura do documento, antes do React, e sai sozinho depois do pointerup, então um toque sem clique não deixa nada engolido para trás.

- `usePresence(open)` devolve `present`, `state` e `onAnimationEnd`. `open` é a intenção; `present` é se a camada ainda existe na tela; `state` vai para `data-state` e é o que o CSS lê para escolher entre entrar e sair. A camada fica montada até o `animationend` da saída, com um temporizador de segurança caso ele não chegue. O ajuste de `present` ao abrir é feito durante o render, o padrão que o React recomenda para reagir a prop nova, e não em efeito, que o lint barra.
- `onAnimationEnd` vai só no elemento animado e ignora o que subiu dos filhos: o evento de animação borbulha, e um giro ou um badge lá dentro fechava a camada antes da hora.
- `layerMotion` é o gênio da lâmpada na versão que o CSS alcança: a caixa nasce a 90% e um degrau mais perto de quem a abriu (`--genie-x` e `--genie-y` dizem de onde) e cresce até o lugar com a mola curta da casa; ao fechar volta pelo mesmo caminho, mais rápido e sem mola, porque saída não pede atenção. Com movimento reduzido sobra só o fade: a regra é sobre deslocamento, e opacidade não desloca nada.
- Cada moldura tem o seu caminho: as caixas coladas no gatilho usam o gênio com a origem no lado do gatilho; a janela centralizada nasce um degrau abaixo e sobe; a gaveta lateral entra e sai deslizando pela lateral; a bandeja do celular sobe do rodapé e volta para ele; a tela cheia do menu sobe um degrau e desbota. Fundo que escurece e véu leve fazem fade nos dois sentidos.
- Quem monta uma camada com `Dialog` precisa mantê-la montada enquanto `open` é falso, senão a saída não acontece. A busca, que era montada só ao abrir, passou a ficar sempre montada com uma chave que muda a cada abertura: a chave remonta o conteúdo, então o estado nasce limpo sem escrever estado em efeito, e o cookie das recentes é lido só quando há documento, porque agora ela também renderiza no servidor.
- Troca de seleção dentro das camadas também anda: o traço da aba de notificações cresce a partir da esquerda, a lista troca com um fade curto quando a aba muda, a opção de tema desliza cor e sombra, e as linhas de lista já transitavam o fundo.

### Hover dos controles

No toque não há hover nem anel de foco (decisão de 2026-09-07): toda regra `:hover` e `:focus-visible` da casa vive dentro de `@media (hover: hover)`, que só vale onde há ponteiro de verdade, então no celular sobra só o `:active`. Foi um pedido do usuário e é um desvio consciente da regra de foco visível, restrito a aparelhos sem ponteiro fino; um teclado ligado a um celular perde o anel. Quem escrever hover ou foco novo segue a mesma receita.

Receita única em `hoverMotion` (`components/ui/styles.ts`), aplicada ao `Button` e a toda linha, pílula, aba e opção das camadas do menu; as linhas do menu em CSS Module repetem a mesma receita, porque módulo não importa mixin do Emotion.

- A transição é assimétrica de propósito: entrar no hover leva o tempo base, para a cor chegar macia, e sair leva o tempo curto, para o ponteiro que passa por uma lista não deixar rastro. A duração maior mora na regra `:hover`, então ela só vale na ida.
- Fundo, cor, borda, sombra e opacidade transitam juntos. Antes só o fundo transitava e a cor do rótulo saltava por cima dele.
- O clique afunda um fio (`scale(0.985)`, 60ms) e volta, o retorno tátil que o Mac dá. Desabilitado não afunda.


### Janela (Dialog)

Moldura da casa para conteúdo que interrompe: caixa centralizada no desktop e bandeja subindo do rodapé abaixo de 48rem, na mesma decisão do `DatePicker`. Portal em `document.body`, `role="dialog"`, Escape fecha, o Tab dá a volta por dentro da caixa e o foco volta para quem abriu. Rolagem trava enquanto está aberta e `prefers-reduced-motion` corta a animação.

- **A trava de rolagem pega a coluna, e não o documento** (correção de 2026-09-08, sobre um relato de que às vezes o celular saltava para o topo ao abrir uma janela). Na concha da aplicação o documento nunca rola: `.shell` tem `height: 100dvh` e quem tem `overflow-y: auto` é a coluna de conteúdo. A `Dialog` e o `DatePicker` declaravam `overflow: hidden` no `html`, o que não travava nada e ainda causava o pulo: no Safari do iPhone a rolagem do documento é a mesma que o navegador usa para esconder a barra de endereço, então travá-la com a barra recolhida faz o Safari trazer a barra de volta e zerar o deslocamento, o que aparece como a página saltando. Era intermitente porque dependia de a barra estar recolhida. Trocar `overflow` de um elemento que rola por dentro não mexe no `scrollTop` dele nem no visor. A coluna se marca com `data-scroll-container` (`lib/scroll.ts`), `useScrollLock` (`hooks/use-scroll-lock.ts`) trava quem estiver marcado e cai no documento nas telas sem a concha, e a contagem é compartilhada porque camada abre de dentro de camada: só a primeira trava e só a última destrava.
- A constante do atributo mora em `lib/scroll.ts`, e não junto do hook: a concha é Server Component e o hook é de cliente, e **constante importada de um módulo `"use client"` chega ao servidor como referência de cliente, não como o valor**. O atributo simplesmente não saía no HTML, sem erro nenhum. Vale para qualquer constante compartilhada entre os dois lados.

- A bandeja do celular sempre põe alguma coisa atrás de si: o escurecimento cheio quando a janela bloqueia o resto, e um véu leve (`--color-scrim-soft`) quando ela é avulsa. Sem nada atrás, a bandeja lia como parte da página. No desktop, dispensar o escurecimento continua deixando a tela limpa.
- Janela de vidro carrega a própria escuridão: ela vai para a sombra do painel, sem desfoque e com espalhamento de `100vmax`, que cobre tudo em volta e nada atrás do vidro, e o fundo fica transparente só para pegar o clique. Pintada no fundo, atrás do vidro, o borrão a puxava para dentro e a bandeja inteira escurecia junto, lendo como se o véu estivesse por cima dela. Subir e descer também desbotam, porque a escuridão agora anda com a bandeja e precisa sumir com ela.
- `scrim` decide o peso da janela. Com ele (o padrão) o fundo escurece em `--color-scrim`, a janela é `aria-modal` e o clique no fundo fecha; a moldura que centraliza cobre a tela e não recebe ponteiro, então o clique na área vazia atravessa e chega lá. Sem ele a janela não bloqueia o resto, não se anuncia como modal e passa a fechar por toque fora da caixa.
- Canto declarado direto no CSS, sem `data-squircle`: a janela guarda foco e conteúdo que sai do fluxo, e o recorte do fallback cortaria o anel de foco de quem está dentro. Mesma escolha do `Listbox` e do `Tooltip`.
- Tamanhos por `data-size`: `sm` 24rem, `md` 30rem, `lg` 40rem. A altura é limitada por `min(34rem, 100dvh menos 4rem)` no desktop e 85dvh na bandeja, e quem rola é o conteúdo de dentro, nunca a página.
- **Fila de camadas** (`hooks/use-layer.ts`, 2026-09-08): janela, gaveta, bandeja e caixa colada no gatilho entram todas numa pilha só, porque uma abre de dentro da outra. `useLayer(aberta)` registra e `isTopLayer(id)` diz quem responde. A `Dialog` só atende Escape quando é a de cima, e o `DropdownMenu` registra o popover do desktop; no celular quem registra é a bandeja dele, que já é `Dialog`. Antes a pilha era local da `Dialog` e o popover não existia nela, então com o menu de opções aberto por dentro do perfil do cliente o Escape fechava os dois.
- **Fundo só fecha o que a pessoa mirou**: a janela guarda, no `pointerdown`, se era a camada de cima naquele instante, e o clique no fundo só fecha se era. É o que separa uma camada da outra no toque: tocar fora do menu de opções fecha o menu, e o perfil embaixo fica de pé, porque quando o dedo desceu quem mandava era o menu. O engolidor de clique do `useOutsideDismiss` continua, mas deixou de ser a única garantia, que dependia de ordem de propagação e de qual elemento estava sob o dedo depois do toque.
- **Arrastar a alça fecha a bandeja** (2026-09-08, a pedido): segurar na barrinha do topo e puxar para baixo leva o painel junto por `translate` inline, escrito uma vez por quadro dentro de `requestAnimationFrame`. São duas escolhas de fluidez, feitas no mesmo dia depois de o arrasto sair travado: `translate` e `transform` são propriedades independentes, e as animações de entrada e de saída da bandeja andam em `transform`, então escrever numa não disputa com a outra nem obriga o navegador a resolver de novo, a cada quadro, o quadro-chave implícito da animação que segura o painel no lugar; e o ponteiro dispara bem mais de sessenta eventos por segundo, então agrupar a escrita num quadro corta recálculo de estilo que a tela nem chegaria a mostrar. O painel também ganha `will-change: translate` enquanto o dedo está nele, porque a bandeja de vidro carrega borrão e uma sombra do tamanho da tela, e sem a promoção a camada própria o navegador repintaria os dois a cada quadro. Soltando além de um terço da altura, com teto de 120px, a janela fecha e a animação de saída continua de onde o dedo largou, em vez de saltar para o lugar antes de descer; aquém disso ela volta com a transição curta da casa. A alça desenha 4px, mas quem pega o dedo é a área invisível em volta, na medida de toque da casa, e ela declara `touch-action: none` para o navegador não rolar a página no meio do gesto. Continua `aria-hidden`: é atalho de gesto, e Escape, o X e o fundo já fecham.
- `focusOnOpen={false}` para o foco no próprio painel, e não no primeiro campo: no celular focar um campo faz o teclado subir sozinho e comer metade da bandeja antes de a pessoa ver o conteúdo. O Tab continua preso dentro da janela, porque o painel carrega `tabindex="-1"`. A prop não se chama `autoFocus` porque o lint barra esse nome em JSX.
- Janela e gaveta usam o mesmo canto, o raio `3xl`, porque são a mesma família e destoavam quando a centralizada ficava num raio menor. A bandeja continua no `xl`, que é o degrau da casa numa tela estreita.
- `placement="end"` troca a caixa centralizada por gaveta na lateral final, entrando por `translateX`, com 8px de folga nos quatro lados (o recuo mora na moldura) e canto completo, porque ela não encosta em borda nenhuma. No celular a bandeja continua ganhando, porque a regra dela vem depois e vence no que declara.
- `surface="glass"` troca a superfície opaca pelo vidro. Há duas receitas em token: `--glass-bg` e `--glass-blur` (fundo a 0% e `blur(12px)`) são as do painel do login e da barra flutuante, que têm pouco conteúdo e podem ser quase invisíveis; `--glass-layer-bg` e `--glass-layer-blur` (fundo a 20% e `blur(16px)`) são as das camadas com lista e texto por cima, que é o que a janela e os popovers usam, para o que passa atrás não competir com o que está escrito.

### Troca de time

A seta dupla no topo do menu abre uma caixa com busca, os times da pessoa e o convite de criar no rodapé, no formato da referência (a troca de projeto da Vercel).

- Sem escurecer a tela e colada na origem (decisão de 2026-09-03): no desktop é caixa avulsa de 20rem medida a partir do próprio seletor, alinhada pelo início dele, virando para cima quando falta espaço embaixo e acompanhando rolagem e redimensionamento. A medida sai de `useLayoutEffect`, e não de `useEffect`, senão a caixa pinta no canto e pula para o lugar no quadro seguinte. No celular ela vira a bandeja do `Dialog` com `scrim={false}`, porque lá o topo do painel fica longe do polegar.
- A faixa dos times tem altura reservada, de 10rem a 15rem no desktop e até 22rem na bandeja, e é ela que rola. Sem piso, um time só deixava a caixa espremida entre a busca e o rodapé; sem teto, muitos times empurravam o convite de criar para fora da vista.

- Regra no banco: `set_current_org` já existia, é `security definer` e recusa organização de que a pessoa não participa. A web chama por `switchTeamAction` e o aplicativo por `PUT /api/v1/organizacoes/atual`, os dois pela mesma função, então a validação não é repetida em código.
- A lista sai de `listTeams`, que entra pela associação (`organization_members`) e não por `organizations`: a policy de select de lá também deixa passar o time que a pessoa criou, então quem saiu do time continuaria vendo um destino que a troca recusa. O plano de cada linha vem de uma leitura em lote de `organization_subscriptions` com os status de `grantingStatuses`, a mesma lista de `organization_plan`. O aplicativo lê em `GET /api/v1/organizacoes/minhas`.
- O foco fica no campo de busca o tempo todo: as setas movem o item ativo, Enter escolhe e o leitor de tela acompanha por `aria-activedescendant`, então as linhas são `role="option"` e não botões. A busca compara por `slugify` dos dois lados, então acento e maiúscula não atrapalham.
- Escolher um time chama a action, mostra giro na própria linha e refaz a árvore do servidor com `router.refresh()`. Enquanto a troca corre a janela não fecha, nem por Escape.
- A caixa é de vidro nos dois formatos, na mesma receita do painel do login: `--glass-bg` e `--glass-blur`.
- O convite de criar não encosta na borda do rodapé: o recuo é o mesmo da lista, então o hover pinta uma caixa arredondada por dentro, no ritmo das opções de cima, em vez de uma faixa de ponta a ponta.
- Criar equipe fecha a caixa e abre a gaveta de criação, descrita abaixo.

### Busca (CommandPalette)

O campo de busca do menu e o "Buscar" da barra flutuante abrem a mesma janela: campo em cima, atalhos em pílula, o que foi aberto por último e sugestões. Caixa de vidro `md`, centralizada no desktop e bandeja no celular.

- As opções saem de `nav.ts`, que já é a fonte única do menu e tem `href` tipado por rota, então a busca nunca oferece página que não existe. `navLinks()` achata as pastas e carrega o nome de onde a página mora, porque "Acompanhar" e "Visão geral" não dizem nada fora da gaveta; `navHighlights` é o punhado que vira pílula, com rótulo próprio.
- A pílula tinge pela mesma receita do Badge: tinta no matiz a 70% do rótulo e fundo no matiz em alfa baixo, mais forte no escuro, com o contraste já conferido nas 19 cores da paleta. O matiz de cada atalho fica em `navHighlights`, e o ícone herda a tinta em vez de puxar para o cinza. Canto pelo sistema da casa, no raio `md`, e não pílula redonda.
- A fila de atalhos rola na horizontal com a barra de `thinScrollbar`, e não escondida: a régua diz que a fila continua. Quebrar em duas linhas empurrava a lista para fora da vista numa tela estreita. O recuo dela é o mesmo da linha da lista, senão as duas faixas começam em pontos diferentes.
- As duas áreas que rolam desbotam nas pontas por `mask-image`, e não por uma faixa de cor por cima: o painel é de vidro, e um gradiente sólido viraria mancha em vez de sumiço. Em cima a faixa tem o tamanho do recuo, então cai sobre o vão; embaixo ela é maior, porque ali a lista encosta na régua de teclas e o corte seco aparecia inteiro.
- A pílula fica em 36px no toque, e não no alvo cheio: ela é atalho secundário, a lista logo abaixo é quem guarda os 44px da regra, e com 44 aqui a faixa do topo comia um terço da bandeja.
- Cada linha da lista é ícone, rótulo e a seção em etiqueta colorida na outra ponta. Sem seta de abrir no fim: a janela inteira já é uma lista de coisas que abrem, e o ícone repetido em toda linha só somava ruído.
- O matiz da etiqueta é o da seção, declarado em `nav.ts` na pasta e no grupo e propagado por `navLinks()`. Cor de seção mora onde a seção nasce, e não numa tabela paralela que envelhece quando o menu muda.
- A linha da lista tem a altura do menu, 36px com 4px de recuo, e volta ao alvo de toque em ponteiro grosso. Entre um grupo e outro corre a mesma linha que separa os grupos do menu, sangrando o recuo da lista.
- A régua de teclas no pé diz o que dá para fazer daqui: fechar, navegar e abrir. Ela e as próprias teclas somem no celular.
- O rótulo de grupo recua o mesmo que a opção. Ele mora dentro da lista, que já abre o recuo lateral, então o recuo cheio somava duas vezes e jogava "Recentes" e "Sugestões" para dentro.
- Com o campo vazio a janela mostra Recentes e Sugestões; com texto, uma lista só de Resultados, filtrada por `slugify` no rótulo e na seção, então acento e maiúscula não atrapalham.
- Recentes são as três últimas rotas abertas pela busca, em cookie (`sp-recent`), porque Web Storage é proibido pelo lint. Guarda só a rota, que já é pública e conhecida pelo menu, nunca o que foi digitado.
- Um `listbox` só, com o rótulo de cada grupo como item de apresentação dentro dele: um listbox por seção duplicaria o id que o campo aponta em `aria-controls`. O foco fica no campo, a seta move o item ativo e o leitor de tela acompanha por `aria-activedescendant`.
- Quem abre monta o componente, e não o mantém montado com `open={false}`: assim o estado nasce limpo a cada abertura e o cookie é lido uma vez, sem escrever estado dentro de efeito.
- O botão de busca no menu mostra o atalho inteiro, modificador e letra, e não só a letra. O rótulo do modificador vem de `useCommandKey`, que responde Ctrl no servidor e troca para ⌘ na hidratação em Mac, então a marcação não discorda. O atalho é o comando mais a tecla que o campo mostra. Ele toma o lugar da busca do navegador de propósito: procurar no texto da página não serve a quem quer pular para outra tela. A régua de teclas e a própria tecla somem no celular.

### Opções da conta

O chevron duplo do perfil abre a lista colada nele, que sobe porque o perfil mora no rodapé do menu: quem é você em cima, as telas da conta no meio, o tema e a saída embaixo. Vidro, fio da casa e a mesma linha do menu, na mesma altura e no mesmo tom, porque a camada é continuação dele.

- Assinatura leva o chip do plano em vigor, o mesmo rótulo curto que o topo do menu mostra.
- Tema em dois estados na própria linha, sol para o claro e lua para o escuro, com a fila na frente do rótulo como no celular. O apertado é o que tem fundo, sombra e o glifo preenchido; o nome de cada um fica em `aria-label` e em `title`, e o rótulo da linha usa o meio círculo para não repetir a lua. "Sistema" saiu: o produto é escuro por padrão e não segue o aparelho, então a terceira opção só confundia e estourava a linha estreita do celular. O estado do seletor nasce do que o html carrega, e não do cookie, porque sem cookie o produto é escuro. A troca chama `applyTheme`, que saiu do `ThemeToggle` de homologação para `lib/theme.ts` quando os dois passaram a precisar dela.
- Sair é `<a>` cru, e não `Link`: limpar o cookie de sessão exige uma ida ao servidor, coisa que navegação de cliente não faz.
- O painel só existe no desktop, mas as opções são as mesmas nos dois: `accountLinks` e `ThemePicker` saem daqui e a tela cheia do celular monta as mesmas linhas com o CSS dela. As molduras diferem, a lista não, então nenhuma das duas conta uma história diferente da outra.

### Menu de opções (DropdownMenu)

`DropdownMenu` (`components/ui/dropdown-menu/`, 2026-09-07) é o menu de opções colado no próprio gatilho, no padrão das camadas do menu lateral, feito para o chevron de cada item de lista (o primeiro uso é o cliente, sobre uma referência do usuário).

- Gatilho é o `IconButton` fantasma com o chevron duplo (ou o ícone passado), com `aria-haspopup` e `aria-expanded`. No desktop abre uma caixa de vidro de 280px colada no gatilho: o `useAnchoredPosition` estima o lado pela contagem de itens antes de pintar e, no mesmo quadro, um `useLayoutEffect` mede a caixa já montada e a encaixa na janela de verdade, abaixo do gatilho se couber, acima se couber melhor, e presa às bordas com 16px de folga quando não cabe de jeito nenhum, com a rolagem interna da caixa cuidando do resto (ajuste de 2026-09-07: a estimativa sozinha cortava a caixa em cima quando ela virava para cima perto do pé da tela). A posição chega em pixels resolvidos, sem `translate` para o lado de cima, e o gênio de entrada e saída das camadas nasce do lado do gatilho; fecha por toque fora (`useOutsideDismiss`). No celular é a bandeja do `Dialog` em vidro e sem escurecimento, a mesma decisão da troca de time, com a lista rolando por conta própria dentro do painel, que esconde o que transborda (sem isso ela era cortada no fim); o vidro da bandeja subiu de 40% para 72% de fundo em `--glass-sheet-bg`, porque sobre o painel o menu ficava difícil de ler.
- Seções (`DropdownSection`) separadas pelo fio da casa, com título opcional em `caption1` ("Acompanhamento"). Itens de ação (`DropdownAction`) viram botão, `Link` para rota da casa ou `<a>` em outra aba para endereço externo, na linha do menu (36px, 44 no toque, hover no preenchimento da casa), com ícone à esquerda e, no fim da linha, a contagem em `Badge` neutro ("12"), o selo do plano que libera no mesmo chip neutro e suave da Assinatura no menu da conta ("Pro"; o roxo sólido durou uma rodada) e a seta de mais opções quando há submenu; `tone="danger"` pinta a linha em vermelho ("Excluir"). Itens de interruptor (`DropdownToggle`) são um `label` com o `Switch` pequeno no fim, então clicar em qualquer ponto da linha alterna; a linha é `menuitemcheckbox`.
- Escolher uma ação fecha o menu; alternar um interruptor não. Ação com `keepOpen` também não fecha: é para menu de filtro, em que a pessoa ajusta várias coisas antes de sair (2026-09-08). O gatilho é fantasma por padrão e aceita `trigger`, um `Pick` de variante, canto e cores do `Button`, que é como a barra de busca veste os dela. Ao abrir, o foco vai para o primeiro item; setas, Home e End andam pelos itens, Escape fecha e devolve o foco ao gatilho. Os itens ficam fora do Tab (`tabindex="-1"`) para a navegação ser só por setas.
- O submenu é só a seta por enquanto: a opção age como ação simples. Quando existir, abre outra camada ao lado.
- `ClientMenu` (`features/clients/components/client-menu.tsx`) monta as opções de um cliente no padrão pedido: Visualizar (`/clientes`), Editar, Gerar orçamento (`/orcamentos/novo?cliente=<id>`), Gerar cobrança (`/cobrancas`) e Gerar contrato (`/contratos`), os três documentos com seta e selo Pro, e Chamar no WhatsApp (`wa.me` com o telefone, só quando há telefone); seção Acompanhamento com Histórico (contagem) e Mapa de relação; interruptores Ativo e Favoritar; e Excluir em vermelho. Editar, histórico, mapa e excluir ainda não têm tela nem regra e só fecham o menu; ativo e favorito trocam só na tela.

### Janela de detalhes (DetailsTrigger)

`DetailsTrigger` (`components/ui/details-dialog/`, 2026-09-07) é o resumo em janela de qualquer item de lista: a linha vira o próprio botão (o reset do botão vai dentro de `:where()`, com especificidade zero, para a classe de layout de quem chama continuar mandando) e o clique abre o `Dialog` da casa, `sm`, em vidro, centralizado no desktop e bandeja no celular, com título, subtítulo, ficha em duas colunas e uma nota. A linha continua sendo o elemento que era, `li` ou `div`, com as mesmas classes: o gatilho só lhe dá papel de botão, foco por teclado (Enter e Espaço), o cursor e o anel de foco; um clique que nasce num controle de dentro (o menu do cliente) não abre a janela, a menos que o controle traga `data-open-details`, como a seta do cartão de tarefa. A janela aceita também conteúdo próprio (`dialog`), com só o X de fechar entrando por fora, para quem precisa de mais que a ficha padrão. Onde entra: no financeiro a linha da movimentação abre o recibo (`TransactionReceipt`, em `features/finance/components/`, sobre uma referência de comprovante do usuário, 2026-09-07): em cima quem está do outro lado em 52px com o selo da situação na quina (verde com check confirmada, laranja com relógio aguardando, vermelho com X cancelada), o que aconteceu em uma linha ("Recebimento confirmado", "Pagamento realizado", "Recebimento previsto", "Pagamento cancelado") e de quem; depois identificador (`TRX-2026-0147`, prefixo novo em `lib/utils/reference.ts`) e data e hora; a linha tracejada de recibo no fio da casa; categoria, descrição, valor com sinal (sempre neutro, como na lista) e a situação em `Badge`; e a forma de pagamento num cartão no preenchimento da casa, com ícone (Pix, cartão, boleto, transferência). Nas tarefas o cartão abre a ficha completa (`TaskSheet`, em `features/tasks/components/`, sobre referências de ficha de consulta e de reunião do usuário, 2026-09-07, na janela `lg` por `dialogSize`, que para esse tamanho ficou mais alta, até 46rem): em cima o projeto e o identificador (`TAR-2026-0031`, prefixo novo) em `caption1`, o título em `title3` e as etiquetas de situação, prazo (com calendário) e prioridade (bandeira em quatro tons: Baixa neutra, Normal em `info`, Alta em `warning`, Urgente em `danger`); um aviso em fundo laranja suave quando a tarefa tem algo a ler antes de mexer; os fatos em rótulo com ícone à esquerda (8rem, e em linha própria abaixo de 30rem) e valor à direita, com Responsável e Envolvidos em chips de pílula com a bolinha e o nome, Prazo e Início por extenso, Estimativa em horas e minutos, Projeto em chip com o identificador e Etiquetas em `Badge`; e, depois do fio, as seções de Descrição inteira, Subtarefas (`Subtasks`, Client Component: conta "2 de 4" com uma `Progress` de um segmento por subtarefa, e cada uma com a `Checkbox` da casa para marcar e desmarcar, riscada quando marcada, e a bolinha de quem cuida na ponta; o estado é só da janela enquanto o domínio não está no banco), Anexos (`AttachmentCard`, Client Component: cada um é um botão em cartão no preenchimento da casa com o ícone do tipo, nome, "PDF, 937 KB", a situação em `Badge` e a seta na ponta, e abre o arquivo na própria tela, numa janela `lg` de vidro por cima da ficha, com o palco em 16:10 mostrando a imagem por `next/image`, o PDF em iframe (o `frame-src` da CSP ganhou `'self'` para isso) ou o endereço do link, e um container flutuante no rodapé do palco, no vidro do toast e do menu e no raio `lg` (abaixo da metade dos 44px de altura), com as ações: baixar, abrir em nova aba, copiar o link (com toast) e fechar; a `Dialog` ganhou uma pilha de janelas abertas para só a de cima responder a Escape e prender o Tab) e, depois de um fio que separa do resto, Atividade (bolinha, primeiro nome em medium e a ação, a hora embaixo). A fonte da ficha subiu um degrau em tudo para ler melhor no desktop (título em `title2`, seções em `callout`, valores em `subheadline`, legendas em `footnote`), com 24px de recuo a partir de 48rem. As etiquetas de situação, prioridade e prazo ficaram em `features/tasks/labels.ts`, compartilhadas pelo bloco e pela ficha. O modelo ganhou identificador, prioridade, responsável, início, estimativa, projeto, etiquetas, aviso, subtarefas, anexos e atividade. Quem está do outro lado saiu do bloco para `TransactionParty` (`features/finance/components/`), em dois tamanhos, o da linha e o do recibo. O modelo ganhou identificador, hora, situação (sem valor, sai do tipo) e forma de pagamento; nos clientes a linha da lista e o destaque inteiro abrem o perfil (`ClientProfile`, em `features/clients/components/`, sobre referências de cartão de perfil do usuário, 2026-09-07, na janela `lg`), montado sobre o cartão de perfil da casa (`Profile`, em `components/ui/profile/`, com `ProfileSection`, `ProfileRule`, `ProfileFacts`/`ProfileFact`, `ProfileTags`, `ProfileList`/`ProfileRow` e `ProfileProgress`; CSS Module e sem diretiva de cliente, para servir ao servidor e a quem já é cliente): a capa colada no topo no matiz da pessoa (o mesmo do rosto gerado, por `avatarHue`, que saiu do `Avatar` para isso), cheia em cima e sumindo para o fundo, com um brilho radial na quina; a foto em 5,5rem e raio `xl` passando por cima da borda da capa, com anel na cor do fundo; o nome em `title2` com as etiquetas "Novo cliente" (até sete dias), Ativo ou Inativo e Favorito (estrela em amarelo), e o `ClientMenu` na outra ponta (menus, listas, dicas e o calendário subiram para a camada `--z-popover`, 450, acima das janelas, porque abrem de dentro delas); o e-mail como identificador e "Estúdio Aurora, Design de interiores" embaixo; os números da relação em quatro colunas (orçamentos, projetos, faturado e em aberto em `compactMoney`); os botões de contato (WhatsApp em nova aba na cor oficial da marca, `--color-whatsapp` e `--color-on-whatsapp`, tokens novos, pelo `background` e `foreground` do botão, cujo hover agora sai da cor passada; E-mail e Ligar em contorno, por `mailto:` e `tel:`); e as seções Sobre (anotações da equipe), Contato em rótulo com ícone e valor (e-mail, telefone com máscara, cidade, site como link, cliente desde, identificador `CLI-2026-0026`, prefixo novo), Etiquetas em `Badge`, Orçamentos vinculados (linhas que levam a `/orcamentos/[id]`, com ícone, título, número e data, valor e a situação no mapa compartilhado `features/quotes/labels.ts`, que o bloco de último orçamento passou a usar também; "Ver todos" na cabeça) e Projetos (linhas para `/projetos/[id]` com barra de progresso e situação). O modelo ganhou identificador, empresa, atividade, cidade, site, anotações, etiquetas, ativo, favorito, números, orçamentos e projetos. Na equipe, a faixa de quem está em foco (e a seta na ponta dela) abre o perfil da pessoa (`MemberProfile`, em `features/organizations/components/`, sobre o mesmo cartão de perfil, 2026-09-07): etiquetas do papel de acesso (Proprietário em amarelo com coroa, Administrador em `info` com escudo, Membro neutro) e da situação (Ativo, ou Convite pendente), os pontos de gamificação em etiqueta na outra ponta, o e-mail e a função; os números do que a pessoa produziu (entregues, faturamento, em andamento, tarefas abertas); WhatsApp, E-mail e Ligar; e as seções Sobre, Contato (e-mail, telefone, cidade, na equipe desde ou convidado em, acesso), Habilidades, Projetos em que está (com progresso) e, depois de um fio, Atividade recente. Convite pendente mostra um aviso no lugar dos projetos. O bloco da equipe é cliente, então o perfil vai junto no pacote dele; a faixa é `styled(DetailsTrigger)`, que repassa `as` e as props da janela. O modelo de membro ganhou acesso, e-mail, telefone, cidade, entrada, apresentação, pontos, habilidades, mais duas medidas (em andamento e tarefas abertas), projetos e atividade. A ficha padrão em texto continua disponível para quem não precisa de mais. Rodada do celular nos perfis (2026-09-07): as ações passaram a ser dados do cartão (`ProfileAction`), renderizadas em dupla, o botão com texto e o só de ícone, com a largura escolhendo qual aparece, no raio `md`; os tokens de texto são redeclarados dentro do cartão no celular para tudo descer um degrau; os fatos viram células em duas colunas; as linhas de orçamento e projeto ficaram em grade, com a ponta caindo para baixo do título sem mexer o chevron; e a capa sobe por baixo da alça da bandeja (`DetailsDialog` com `flush`, e `dialogFlush` no gatilho; a alça da `Dialog` ficou acima do conteúdo). O fundo da `Dialog` subiu para a camada da janela (vem antes da moldura na árvore, então fica atrás da própria janela e acima da de baixo), e o clique fora chega ao fundo certo (o visualizador do anexo não fechava ao clicar fora; uma versão com fundo e moldura numa camada só durou uma rodada); e o `useOutsideDismiss` espera 200ms pelo clique depois do dedo sair, porque no toque ele chegava depois do tique seguinte e vazava para a janela de baixo, fechando o perfil junto com o menu. A ficha da tarefa desce um degrau no celular pelos mesmos tokens e põe os fatos em duas colunas; o visualizador do anexo ganha palco em pé com 64dvh e botões de 32px. Reparo da fila de ações e das duas colunas (2026-09-08): as duas peças da mesma ação apareciam juntas no celular, o ícone do WhatsApp ao lado do botão com o nome escrito, e a fila de seis botões estourava a largura da janela, que é de onde vinha a rolagem lateral. O motivo é especificidade: o `Button` declara o próprio `display` em `&[data-full-width]`, que empata com duas classes e vencia por ser inserido depois pelo Emotion, então a regra que escondia o botão com texto não valia. Agora quem manda tem quatro degraus (`.actions .action[data-primary] .wide`), e a regra mudou junto: no celular só a ação principal mantém o texto, marcada por `data-primary` no invólucro (é o WhatsApp nos dois perfis), enquanto E-mail e Ligar ficam só com o ícone; a principal cede largura e as de ícone não encolhem. Contra a rolagem lateral entraram duas guardas: a coluna dos grids do perfil e da ficha da tarefa passou a ser `minmax(0, 1fr)`, porque a trilha automática tem o min-content do conteúdo como piso e um e-mail comprido alargava a janela inteira, e o valor do fato ganhou `overflow-wrap: anywhere`, que ao contrário de `break-word` também encolhe a largura mínima do item de flex. As duas colunas dos fatos viraram `repeat(auto-fit, minmax(9rem, 1fr))` nos dois lugares, então em 320px fica uma coluna só e as duas voltam a partir de 375px; e na ficha da tarefa Responsável, Envolvidos, Projeto e Etiquetas tomam a linha inteira por `wide`, porque o valor deles é objeto de largura própria e em meia coluna cada chip caía numa linha, o que quebrava a ficha em escada. Em duas colunas ficaram só os fatos de texto simples, que é onde a divisão ajuda. Segunda rodada no mesmo dia, a pedido: na ficha da tarefa os fatos viraram **uma coluna só** no celular, porque o valor de quase todos ali é objeto de largura própria e meia coluna deixava a ficha em escada; os envolvidos passaram a caber numa pílula só, na receita do aviso do menu lateral (até três bolinhas sobrepostas e o resto em "+N", com os nomes inteiros na leitura por voz), no lugar de uma pílula com nome por pessoa, que empurrava a ficha para várias linhas assim que a tarefa tinha mais de duas; e as linhas de orçamento e projeto e o cartão de anexo passaram a pôr ícone e chevron na linha do título, se centrando por ela, com o que estava na ponta descendo para a coluna do texto. O ícone esticado pelas duas linhas boiava no meio do cartão, longe do título que nomeia, e nada é esticado agora, então linha sem ponta não abre uma segunda faixa vazia. A etiqueta do anexo desceu junto, porque na mesma linha do nome ela roubava a largura dele e sobrava reticência quase inteira. No perfil do membro a função saiu do topo e virou o primeiro fato de Contato, e o e-mail saiu de vez do topo: ali ele repetia o que a própria seção de contato mostra logo abaixo. Projetos ganhou um fio antes, como Atividade já tinha. Terceira rodada do dia, de caprichos: os envolvidos ganharam a fila de nomes ao lado das bolinhas ("Lucas, Douglas, Marcos… +59"), cortada por caractere em `peopleLabel` (`features/tasks/labels.ts`) e não por quantidade de gente, porque "Ana, Léo e Bia" e "Alexandre e Guilherme" têm larguras muito diferentes com o mesmo número de nomes; o primeiro nome entra sempre, mesmo passando do orçamento de 32 caracteres, senão a fila ficaria só com a contagem. O identificador da tarefa saiu de texto solto para `Badge` neutro suave na linha da pasta, para ceder o destaque a ela, que é quem situa a tarefa. E o anexo passou a ter uma cara por tipo, num mapa só (`kinds` em `attachment-card.tsx`: glifo, nome do formato, se baixa e como o palco mostra): `image` desenha a imagem, `frame` embute o PDF em iframe, e `card` monta a ficha do endereço para o que não abre dentro da nossa tela, com a marca do serviço num azulejo, o nome, o domínio numa pílula e a linha que diz para onde o botão leva. O Figma virou tipo próprio (`figma`), e tanto o chip da lista quanto o azulejo da ficha mostram o logo em cores pelo `BrandIcon`, quando a casa conhece o domínio (`brandHosts`, só marcas que existem em `public/brands`); sem marca conhecida entra o globo. Baixar some no que não é arquivo, por `downloadable` no mapa. O Figma não entra em iframe de propósito: o `frame-src` da CSP é `'self'`, e abrir exceção para um serviço de fora não vale a ficha. A fila de fotos da equipe perdeu o piso de 44px no toque (estourava a altura e virava rolagem vertical recortada) e encaixa por aproximação; um `touch-action: pan-x` que travava a rolagem da página durou uma rodada. Os tokens de texto redeclarados no celular vão em valor, um degrau abaixo da escala, porque encadeados encolhiam duas vezes. No Safari do iPhone, onde o fio de duas camadas do `Card` e do invólucro `bare` some nas quinas superelípticas, as duas caixas voltam ao canto redondo por `@supports (-webkit-touch-callout: none)`. As fichas são montadas no servidor em texto, então a janela não conhece domínio nenhum. A primeira versão, que trocava o `li` por um `button` do Emotion, saiu no mesmo dia: o reset do botão passava por cima do recuo e do fundo dos módulos e tirava o padrão visual das linhas.

### Notificações

O sino no rodapé do menu abre a caixa colada nele, que sobe porque ali não há espaço embaixo: cabeçalho com o que falta ler, categorias em aba e a lista. Bandeja no celular, onde o rodapé do menu fica longe do polegar.

- Quatro abas: todas, ação necessária, revisões e sistema. A fila rola na horizontal e desbota nas pontas, como a fila de atalhos da busca, porque quatro categorias não cabem numa tela estreita.
- A aba em vigor é marcada por traço embaixo, e não por preenchimento: preenchimento ali competiria com a marca de não lido de cada linha.
- Não lido acende um ponto na frente do título, e a linha lida cai de opacidade. Pintar a linha inteira transformava a lista num bloco de cor com meia dúzia de itens. Clicar na linha marca como lida.
- O sino carrega o contador do que falta ler na própria quina, virando "9+" acima de nove, e o cabeçalho tem só silenciar e fechar. Silenciar é preferência de interface, então mora em cookie e sobrevive ao recarregar; o sino troca para o riscado enquanto está mudo.
- No celular o sino também aparece na barra flutuante, ao lado do botão do menu, e some de lá quando não há nada sem ler. Os dois gatilhos leem a mesma lista, que mora na moldura do menu: o painel aceita `onChange` e, sem ele, guarda a própria lista, que é como a vitrine o usa.
- O botão dentro da notificação é o `Button` da casa, e não um link pintado à mão.
- A medida da caixa colada no gatilho é o hook `useAnchoredPosition`, o mesmo da troca de time: mede em `useLayoutEffect`, estima a altura em vez de medir, e vira para cima quando falta espaço embaixo.
- A caixa tem altura fixa (`PANEL_HEIGHT`, a mesma que o hook usa para decidir o lado), e a lista preenche o que sobra e rola dentro. Antes o corpo tinha só mínimo e máximo, e o painel encolhia a cada aba com poucos itens. A bandeja do celular guarda a mesma altura, com o teto de tela do `Dialog` valendo por cima.
- **O domínio não existe no banco.** A lista chega por prop e hoje vem de `notifications/preview.ts`. Quando a tabela nascer, muda só a origem: o painel não sabe de onde a lista vem.

### Criar equipe

Gaveta de vidro com 30rem na direita (`Dialog` com `placement="end"` e `surface="glass"`), entrando por deslize, com bandeja no celular. Ela vai sem o fundo que escurece, e não por descuido: o vidro borra o que está atrás dele, e com o escurecimento ligado quem seria borrado é o próprio escurecimento, deixando a gaveta cinza no tema claro em vez de translúcida. Uma lista só, sem etapas: quem cria a segunda equipe já conhece o produto, então o fluxo de primeiros passos, que é guiado, não se repete aqui.

- Cabeçalho enxuto e baixo: só o título, a etiqueta do plano que libera a operação e o X na outra ponta, sem linha de descrição. A etiqueta é o chip neutro de sempre, sem ícone, e o plano sai de `CREATE_TEAM_PLAN`, que é a fonte única: o convite que abre a gaveta mostra a mesma. O rótulo curto virou `planBadges` em `billing/plans.ts`, porque agora três lugares o usam.
- Os fios seguem o menu, e não o padrão genérico: 0,6px em `--color-border`, com a variável nascendo na janela e descendo por cascata para cabeçalho, divisor e rodapé. O `Separator` puro traz `--color-separator`, quase três vezes mais opaco, que numa coluna estreita vira um risco preto no meio do formulário.
- De cima para baixo: identidade (banner com a logo montada na borda de baixo, o mesmo `ImagePicker` dos primeiros passos), nome, site, área de atuação e as pessoas. Cabeçalho e rodapé ficam fixos, e só o meio rola.
- A capa é de perfil de rede social: banner deitado em cima e a bola da logo montada sobre ele, presa na esquerda. Quanto dela fica para fora sai de `--identity-logo-out`: 0,4 no desktop, que deixa 60% da bola dentro, e 0,25 no celular, onde a capa é baixa e ela precisa afundar mais. O par é montado pelo `ImageGroup`, exportado junto do `ImagePicker`, então a gaveta e os primeiros passos usam o mesmo agrupador.
- O transbordo e a margem que o reserva saem os dois do tamanho da bola (`calc` sobre `--identity-logo-size`), e não de números soltos: assim mudar só o tamanho mantém o encaixe em qualquer largura. Cada tela ajusta `--identity-ratio` e o tamanho; a gaveta usa 3 por 1 com bola de 5rem, porque numa coluna de 30rem a bola de 6rem cobria quase toda a capa.
- Os três campos da equipe leem como um bloco: nome na linha inteira e, logo abaixo, site e área lado a lado, com o mesmo vão entre eles.
- O recuo lateral mora em cada bloco, e não na coluna que rola: assim o divisor que abre as pessoas corre de ponta a ponta sem margem negativa, que dentro de `overflow-y: auto` é cortada de um lado e vira rolagem do outro.
- A coluna declara `align-content: start`. Ela cresce para preencher a gaveta e, no `stretch` que é padrão do grid, as linhas de altura automática esticam para dividir a sobra: os blocos abriam como se houvesse space-between, com os campos longe uns dos outros. A leitura é de cima para baixo e a sobra fica no fim. Depois dele vêm o título "Membros", os campos de convite com o botão e, por último, a lista de quem já está, com quem cria no topo.
- O endereço não aparece: ele sai do nome pela regra do domínio e não é editável, então campo travado só ocuparia linha sem dar escolha nenhuma.
- Quem cria já entra na lista de pessoas como proprietário, porque é o que o banco vai gravar. A linha não tem papel para escolher nem ação de remover.
- Os convites entram numa lista local antes de existir a equipe, porque `create_invite` precisa do id da organização. Só depois de a equipe nascer eles saem, com o papel escolhido em cada linha.
- Criar chama `saveTeamAction` e, na sequência, `switchTeamAction`: a equipe nasce e a pessoa já entra nela, então a gaveta fecha com o menu inteiro já refeito. Imagem e convite seguem em segundo plano, pelo mesmo motivo dos primeiros passos: cada envio custa uma ida ao servidor, em série, e segurando a tela isso somava segundos.

### Marca

Logos em `public/logotipo` (icon, logo e logotipo em preto e branco). O favicon em `src/app/icon.svg` deriva do icon preto e inverte no modo escuro. A imagem Open Graph usa o logotipo branco sobre preto.

## Ecossistema web + mobile (mesmo banco)

Haverá app Android/iOS usando literalmente o mesmo Supabase. Consequências que valem desde já:

1. **Regra de negócio no banco primeiro**: RLS, constraints, funções SQL (RPC) e triggers. Web e mobile falam com o mesmo Postgres e recebem as mesmas garantias.
2. **`service.ts` por feature**: lógica que precisa de segredo de servidor (Stripe, Resend, OpenAI, Redis) fica em `src/features/<dominio>/service.ts`. Server Actions (web) e Route Handlers em `src/app/api/v1/*` (mobile) são adaptadores finos sobre o mesmo service. Nunca duplicar lógica entre os dois.
3. **API para o app**: `src/app/api/v1/*` autentica por `Authorization: Bearer <access_token>` do Supabase usando `createClientFromRequest` em `src/lib/supabase/api.ts`. A RLS continua valendo porque o cliente carrega o JWT do usuário.
4. **Auth compartilhada**: Supabase Auth no mobile (deep link `specular://auth/callback` entra em `additional_redirect_urls` quando o app existir). MFA TOTP usa as mesmas APIs. Apple Sign In passa a ser obrigatório quando houver login social no iOS, então a conta Apple Developer entra junto com o app.
5. **Tipos e validação compartilháveis**: `src/types/database.ts` (gerado) e `src/features/*/schemas.ts` (zod) são o contrato. Se o app for Expo/React Native em TypeScript, esses arquivos migram para um pacote compartilhado em monorepo sem reescrever.
6. **Multi-tenant desde o schema**: organizações e membros modelados no banco antes de qualquer feature, porque o app precisará do mesmo contexto de conta.

O primeiro domínio montado nesse formato é `organizations`: `service.ts` recebe o cliente Supabase de fora e não sabe quem chamou, `actions.ts` é a casca com sessão, zod, rate limit e revalidação, e `api/v1/organizacoes` (mais `organizacoes/membros`) é a mesma casca para o aplicativo, com `authorizeRequest` de `lib/api/v1.ts` cuidando de token, teto de requisições e leitura do corpo com limite.

## Cobrança e permissão por plano

Montado em 2026-09-01. Três fontes de verdade separadas, de propósito: **o Stripe guarda o dinheiro**, **o banco guarda quem pode o quê** e **`features/billing/plans.ts` guarda o texto de vitrine**. Nenhuma das três repete a outra, então não existe divergência para reconciliar.

### O que vive no banco

| Tabela | Papel |
| --- | --- |
| `billing_plans` | catálogo: código, nome, degrau (`tier`), dias de teste, se o teste exige cartão, se é pago |
| `billing_prices` | mapa plano + ciclo para o `stripe_price_id`. Sem valor: o valor está no Stripe |
| `plan_features` | catálogo de recursos que o plano libera (`flag` liga e desliga, `limit` guarda teto) |
| `plan_entitlements` | a permissão em si: um recurso por plano, com `enabled` e `limit_value` |
| `organization_subscriptions` | uma linha por organização: plano, ciclo, status, ids do Stripe, período, cartão |
| `billing_trials` | teste gratuito consumido, chaveado por organização e plano |
| `billing_events` | id do evento do Stripe já processado, para reentrega não processar de novo |

**`plan_features` e `plan_entitlements` nascem vazias.** Cada condição de plano entra junto com a tela que a exige, nunca antes. Sem linha, `plan_allows` nega e `plan_limit` devolve zero: negado por padrão.

### Como se define a regra de uma tela

Duas linhas de SQL numa migração de `billing`, e mais nada:

```sql
insert into public.plan_features (key, kind, name)
values ('active_projects', 'limit', 'Projetos ativos');

insert into public.plan_entitlements (plan, feature_key, enabled, limit_value)
values ('free', 'active_projects', true, 3),
       ('pro', 'active_projects', true, null),        -- nulo com enabled = ilimitado
       ('alliance', 'active_projects', true, null);
```

A partir daí a regra vale em qualquer lugar, sem código novo:

- Em policy de RLS: `using (public.plan_within_limit(organization_id, 'active_projects', (select count(*) from public.projects p where p.organization_id = ...)))`.
- Em `service.ts`: `client.rpc("plan_allows", { p_organization_id, p_feature_key })`.
- Na interface: o `Button` já tem `locked` com `plan`, que anexa a etiqueta do plano sem tirar o clique, para o clique levar ao upgrade.

Recurso que não existe em `plan_features` **derruba a chamada com erro**, em vez de negar ou liberar em silêncio: chave escrita errada aparece no primeiro teste.

### Funções de permissão

`organization_plan(org)` é a única que decide plano em vigor, e é o lugar único onde os status com direito estão escritos (`trialing`, `active`, `past_due`). Cancelado, expirado e não pago caem no gratuito sem ninguém mais precisar saber disso. As outras se apoiam nela: `current_plan()`, `plan_at_least(org, plano)`, `plan_allows(org, chave)`, `plan_limit(org, chave)`, `plan_within_limit(org, chave, quantidade)`, `trial_available(org, plano)`, `can_manage_billing(org)`.

### Quem escreve

Nenhuma tabela de cobrança tem policy de escrita. Ninguém troca o próprio plano falando direto com a API, nem o dono do time. As duas portas são:

- `attach_billing_customer(org, cliente)`, aberta para `authenticated`, exige owner ou admin e nunca sobrescreve um cliente já vinculado.
- `sync_subscription(...)`, **revogada de `authenticated`** e concedida só a `service_role`. Só o servidor escreve plano e status, e sempre com o que o Stripe devolveu, nunca com o que o pedido mandou.

### Fluxo de pagamento

O formulário é nosso, o cartão é do Stripe. Saiu o Payment Element (que traz rótulo, caixa, abas e o Link do provedor prontos) e entraram os três elementos avulsos, `CardNumberElement`, `CardExpiryElement` e `CardCvcElement`, cada um dentro de um `FieldShell` com `Label` nosso e mensagem de erro nossa. O que continua sendo do Stripe é só o campo em si, um iframe por campo, e é isso que mantém número, validade e CVV fora do nosso DOM e dos nossos servidores (PCI SAQ A). Campo de cartão de verdade no nosso HTML exigiria certificação PCI DSS nível 1 e liberação do Stripe, então não é caminho.

Dentro do iframe a nossa folha não chega: o que atravessa é o objeto `style` de `use-card-style.ts`, com as cores resolvidas em RGB por uma sonda invisível, porque `light-dark()` não sai pronto de `getPropertyValue` (mesma técnica do `GradientBlinds`). Trocar de tema reconstrói o estilo e o `react-stripe-js` repassa por `element.update`. A fonte entra por `fonts: [{ cssSrc }]` apontando para o Inter do Google Fonts: o arquivo do `next/font` é servido do nosso domínio sem CORS, e o iframe, que é outra origem, não consegue buscar. Falhando, cai no tipo do sistema. O raio e a superelipse ficam com o `FieldShell`, que é nosso, então a caixa do cartão tem o mesmo contorno de qualquer outro campo.

Confirmar é `confirmCardSetup` ou `confirmCardPayment` com o `clientSecret` e o elemento do número; o Stripe junta validade e código sozinho, porque os três vieram do mesmo `Elements`. A autenticação do banco abre num modal do próprio Stripe e a pessoa não sai da tela.

Elemento de cartão avulso monta **sem segredo nenhum**, e é por isso que a prévia e a vitrine mostram o formulário de verdade em vez de um espaço reservado: o `clientSecret` só é usado na confirmação, que a prop `preview` desliga.

Dois caminhos, escolhidos no servidor:

1. **Com teste gratuito** (Pro, 7 dias): cria um SetupIntent, guarda o cartão, e só então cria a assinatura com `trial_period_days`. Criar a assinatura antes daria sete dias de plano pago a quem abandonasse o formulário no meio.
2. **Sem teste gratuito**: cria a assinatura com `payment_behavior: "default_incomplete"` e devolve o segredo de `latest_invoice.confirmation_secret`. O plano só passa a valer quando o pagamento confirma, porque `incomplete` não está na lista de status com direito.

`trial_settings.end_behavior.missing_payment_method: "cancel"` fecha o caso do teste que termina sem cartão. O teste é uma vez por organização e por plano, garantido por `billing_trials`: cancelar e assinar de novo não devolve período grátis.

### Webhook

`api/webhooks/stripe` confere a assinatura com `constructEventAsync` antes de ler qualquer coisa, e então **relê a assinatura no Stripe** em vez de confiar no corpo do evento, porque reentrega fora de ordem gravaria estado antigo por último. `setup_intent.succeeded` é a rede de segurança do fluxo de teste: se a aba morrer depois de confirmar o cartão e antes da action responder, o webhook ativa o plano do mesmo jeito, e passar duas vezes não duplica porque a criação reaproveita a assinatura existente. O id do evento entra em `billing_events` **depois** de processar, senão uma falha de escrita perderia o evento; falha responde 500 para o Stripe reentregar.

### Comandos

| Comando | O que faz |
| --- | --- |
| `npm run stripe:sync` | publica produto e preço de cada plano pago no Stripe e grava o `stripe_price_id` em `billing_prices`. Idempotente: produto tem id fixo (`specular_<plano>`) e preço é achado por `lookup_key`. Preço com valor diferente do catálogo vira preço novo com a mesma chave, e o antigo é desativado dos dois lados, para assinatura em vigor continuar no que foi assinado |
| `npm run billing:probe` | teste ponta a ponta contra o Stripe em modo teste e o banco hospedado, pela mesma porta que o aplicativo usa (`api/v1` com Bearer). Cria usuário e time, assina, confirma cartão de teste, confere banco, cancela, retoma, dispara webhook assinado e apaga tudo no fim |

O `stripe:sync` lê `plans.ts` com `--experimental-strip-types`, para o catálogo continuar tendo uma fonte só.

## Telas

### Painel

O painel abre com a pessoa, e não com o nome da tela: avatar, nome e uma frase curta de boas-vindas à esquerda, e a fila de ações à direita. Primeira tela montada um bloco por vez, a partir de 2026-09-04; por enquanto só o cabeçalho existe.

- `PageHeader` (`components/layout/page-header/`) é o cabeçalho genérico de página: `leading`, título, linha de apoio e `actions`, Server Component com CSS Module. Título e linha de apoio ficam a 2px um do outro, porque são uma identidade só. O vão entre as ações é o do menu, 4px, em qualquer largura: com 8 os ícones liam como soltos. Em tela estreita as ações descem para a linha de baixo em vez de sumir, salvo em `compact`: aí no celular ficam a foto e o título na tela numa linha só, a linha de apoio vai para o leitor de tela, e as ações ficam na mesma linha na versão pequena dos botões (36px, com `--touch-target` rebaixado, a pedido) a 4px uma da outra; o cabeçalho não quebra (`nowrap`), porque a identidade cede (`flex: 1 1 0`) e o título encurta por reticências antes de as ações descerem (ajustes de 2026-09-07; antes o título saía da tela, e depois o nome quebrava o cabeçalho em duas linhas no iPhone). O painel usa `compact`, e o botão de criar orçamento vira só ícone no celular por CSS (os dois existem na marcação, cada largura mostra um), para não saltar na hidratação.
- A frase de boas-vindas sai de `features/dashboard/greetings.ts`, sorteada por pessoa e por dia, e não por carga: o período do painel vive na URL e cada troca refaz a página no servidor, então uma frase nova a cada carga trocaria a cada clique e leria como defeito.
- Ações só em ícone, nesta ordem: buscar (abre a mesma busca do menu), período e a engrenagem de personalizar o painel, que abre a gaveta de ordem e visibilidade dos blocos. Criar orçamento é a ação principal, a única com texto, e leva a `/orcamentos/novo`.
- O período é o `DropdownMenu` da casa com o ícone de calendário no gatilho e o escolhido marcado (`selected`, item `menuitemradio` com o check no fim), a mesma camada do chevron duplo dos clientes, com a posição medida antes de pintar para abrir onde cabe (2026-09-07; até então era o `Listbox` em `iconOnly`, que segue existindo para outros usos). O valor em vigor vai no nome acessível do gatilho. A escolha vai para `?periodo=` por `router.replace`, validada com zod no servidor (`features/dashboard/period.ts`; valor inválido cai em "Este mês"), porque é ela que os blocos do painel vão ler. Na vitrine o `demo` guarda a escolha só na tela.
- `DashboardScreen` (`features/dashboard/components/`) é a tela inteira, cabeçalho e grade, montada pela página com dados do banco e pela prévia `/previa/painel` com os dados de exemplo do menu, dentro da `AppFrame`, que é a moldura do `AppShell` sem o banco. O cabeçalho fica grudado no topo enquanto a página rola (2026-09-07): `sticky` na coluna de conteúdo da concha, que é quem rola, desbotando para o transparente como o cabeçalho do site, e não como faixa sólida (pedido do mesmo dia): o fundo é um degradê da cor da página até o nada e o borrão das camadas (`--glass-layer-blur`) é desbotado por uma máscara que vai de cima a baixo (cheia até 20%, nada em 100%), então cor e borrão somem juntos e o que rola por baixo vai ficando nítido conforme desce; com a máscara só no pé, o borrão cheio fazia o degradê ler como faixa sólida, o que o usuário apontou. Sangra o recuo da tela pelos três lados e o devolve por dentro, com uma faixa extra de 24px embaixo onde o degradê acaba. Degradê, borrão e máscara vivem num pseudo-elemento atrás do conteúdo (`isolation: isolate` no invólucro), e não no invólucro: a lista do período nasce dentro do cabeçalho e desce por baixo dele, e uma máscara no invólucro a apagaria. Parado no topo nada aparece. Fica só acima dos blocos (`z-index: 1`), abaixo de qualquer camada flutuante. A tela traz o próprio recuo, 16 no celular e 24 daí para cima, sem teto de largura: o painel aproveita a tela inteira. A concha continua sem recuo.
- A grade (`dashboard-grid.module.css`) tem oito blocos por áreas nomeadas, e o id de cada bloco em `features/dashboard/blocks.ts` é a área: mover um bloco é mexer só no mapa. Todo bloco tem duas linhas de altura, menos os dois pares que dividem uma coluna: conquistas com projetos, e equipe com desafio diário (o desafio entrou em 2026-09-07, quando equipe passou de duas linhas para uma, a pedido). Uma coluna até 48rem; duas até 72rem, em três pares de blocos altos; três colunas a partir de 72rem (896px de conteúdo ao lado do menu, a largura da referência), no mapa pedido: conquistas em cima de projetos na primeira coluna (conquistas subiu para a primeira posição em 2026-09-07, a pedido, para a gamificação abrir o painel), financeiro e clientes ao lado, e embaixo tarefas e último orçamento, altos, com equipe e desafio diário entre os dois. A linha é fixa em 12,5rem em toda largura (bloco com lista rola por dentro, e não estica a página; no celular e no tablet crescia com o conteúdo até 2026-09-07), e no desktop as quatro linhas dividem o que sobra abaixo do cabeçalho entre o piso de 12,5rem e o teto de 60rem da grade, então bloco nenhum estica a linha e gera rolagem; só em tela mais baixa que o piso a página rola. Vão de 16 entre blocos e 24 entre cabeçalho e grade. Cada bloco leva o ícone da rota correspondente no menu e um atalho tipado por rota para a tela dela. Esse mapa é a estrutura, e não muda: quando a pessoa esconde ou reordena blocos pelo ajuste do painel, a grade entra em `data-custom` e a posição passa a ser calculada por `packDashboard` (`features/dashboard/layout.ts`) na mesma estrutura, andares de duas linhas com um alto inteiro ou dois baixos empilhados por coluna, na ordem escolhida, uma vez por largura (na coluna única do celular a ordem é literal; com mais colunas um alto nunca parte um par de baixos: caindo entre dois, o par fecha primeiro e o alto entra logo depois, a regra do usuário), e o bloco chega com coluna e linha em variáveis (`--c1`/`--r1`, `--c2`/`--r2`, `--c3`/`--r3`) e a altura em `--span`; alto continua alto e baixo continua baixo. Com tudo à vista e na ordem padrão, nada disso vale e a grade é a de sempre (decisão de 2026-09-07: uma versão que trocava o mapa por fluxo automático durou minutos e saiu, porque desmanchava a estrutura).
- Personalizar o painel (`DashboardCustomizer`, `features/dashboard/components/`, 2026-09-07): serve para escolher o que aparece e em que ordem, sem mexer na estrutura, que é sempre a de cima. A engrenagem do cabeçalho abre a gaveta da casa (`Dialog` `end` em vidro, sem escurecimento, bandeja no celular) com a lista dos blocos na ordem da grade, cada um com a alça de arrastar (`@dnd-kit/sortable`, ponteiro e teclado, preso ao eixo vertical e à própria lista), o ícone, o nome e o `Switch` de mostrar. Cada mudança grava o cookie `sp-painel` (`features/dashboard/layout.ts`: ordem e escondidos, lido com zod no servidor, valor inválido cai no padrão, bloco novo entra no fim) e refaz a árvore do servidor, que é quem monta a grade a partir do cookie, então a tela atrás se ajusta com a gaveta aberta. Primeiro uso do `@dnd-kit`, que estava instalado à espera do kanban.
- Peso e velocidade do painel (varredura de 2026-09-07): o Recharts sai do pacote inicial por `next/dynamic` sem SSR num invólucro cliente (`projects-chart-lazy.tsx`), porque o gráfico só desenha depois de medir a caixa no cliente e nunca renderizava no servidor; as ilustrações da gamificação e do desafio declaram `sizes` (96px e 48px), então o otimizador serve versões pequenas em vez do arquivo original de centenas de KB; e o rosto gerado do `Avatar` fica em cache por token no servidor (`shape.ts`, teto de 200), porque a mesma pessoa aparece várias vezes por página e cada desenho custa CPU. Não há efeito em cadeia, temporizador nem animação infinita no painel; as animações são só de opacidade e transformação.
- **Varredura de peso de 2026-09-08**, medida no build de produção servindo `/previa/painel`. O painel entregava **791 KB comprimidos** e passou a entregar **347 KB**, com o HTML caindo de 328 KB para 59 KB e o JavaScript de 464 KB para 289 KB. O que estava pesando, na ordem em que apareceu:
  - **1 MB de rosto embutido** (64% do HTML): resolvido pela rota do `Avatar`, descrita na seção dele.
  - **138 KB comprimidos do cliente do Supabase**, no pacote do layout: `Sidebar` traz o `TeamSwitcher`, que trazia a gaveta de criar equipe, que traz `organizations/upload.ts`, que importava `lib/supabase/client` no topo. O import virou dinâmico dentro da função de envio, que já era `async`, então a biblioteca só chega quando há arquivo subindo de verdade.
  - **56 KB do `@dnd-kit`**: a lista que arrasta saiu para `dashboard-blocks-list.tsx` e entra por `next/dynamic`. A gaveta nasce fechada e a `Dialog` não renderiza o conteúdo enquanto está fechada, então o pedaço só é buscado no clique da engrenagem.
  - **63 KB de zod**, por dois caminhos: `dashboard/layout.ts` e `dashboard/period.ts` carregam zod para validar cookie e parâmetro de URL, mas quem os importava eram a gaveta de personalizar e o cabeçalho, os dois de cliente, e cada um só queria a parte leve. As partes leves foram para `layout-cookie.ts` e `period-options.ts`, e a validação continua no servidor, onde entrada de usuário deve ser validada. Mesma história do `slugify`, que saiu de `organizations/schemas.ts` para `lib/utils/slug.ts` porque a busca e a troca de time só queriam ele.
  - **Stripe Elements, `react-dropzone` e o cartão de perfil de membro** no pacote inicial: `OnboardingFlow`, `CreateTeamPanel`, `MemberProfile` e `StreakSheet` passaram a entrar por `next/dynamic`. Todos abrem por clique ou só na primeira visita, e nenhum renderiza enquanto fechado.
  - **A regra que ficou**: componente de cliente nunca importa de um módulo que carrega zod, cliente de banco ou biblioteca de interação. Quando precisar de uma constante ou de uma função pura que mora ao lado dessas coisas, ela muda de arquivo. O peso não aparece na tela, então só uma medição do build o encontra.
- **O que muda quando o dado vier do banco.** Hoje todo bloco lê de `preview.ts` e recebe o resumo por prop, então o formato não muda com a origem, mas o volume muda. Três coisas a observar quando as tabelas existirem:
  - As listas são limitadas na origem (`SHOWN_CLIENTS`, `SHOWN_TASKS`, `SHOWN_TRANSACTIONS`, todos 5), então a carga da página não cresce com a base. A fila de fotos da equipe **não tem teto** e o `TeamBlock` é de cliente: hoje são 8 pessoas de exemplo, mas o resumo inteiro atravessa a fronteira, com projetos, atividade, habilidades e apresentação de cada uma. Numa equipe grande isso vira o maior item da página, e é o primeiro lugar a ganhar um teto.
  - `WeeklyChallenge.history` tem um dia por dia desde o primeiro acesso e atravessa inteiro para o `StreakButton`, que é de cliente. São 142 dias na prévia e cresce um por dia em produção. A ficha da sequência precisa dele, mas só quando abre; quando o domínio nascer, vale buscar o histórico na abertura em vez de mandar junto.
  - As **18 janelas de detalhe** do painel são renderizadas no servidor e viajam na carga do RSC sem nunca aparecer: medi **22 KB comprimidos e 372 KB brutos**, 38% do HTML que sobrou. Com dado de verdade isso não diminui. Para resolver, as fichas precisam virar componente de cliente carregado por `next/dynamic`, recebendo o dado em vez da árvore pronta; é a maior peça que ficou de fora desta varredura.
- Entrada dos blocos (2026-09-07): o cabeçalho do painel e o menu lateral surgem com um fade curto só de opacidade, e cada bloco surge com um fade e um deslize de 8px de baixo para cima, em cascata pela ordem de leitura, 45ms entre um e outro, então o último dos oito entra 315ms depois do primeiro e a página assenta em pouco mais de meio segundo. A grade escreve `--index` em cada bloco e o CSS lê para o atraso; a animação é só de opacidade e transformação, com `both` para o bloco não piscar antes do atraso, e movimento reduzido a zera pelo reset da casa. Sem JS.
- Bloco `bare` (2026-09-07): o que se declara assim em `blocks.ts` dispensa o `Card` e entra direto na área da grade, num invólucro que desenha o fio e o raio na mesma receita de duas camadas do `Card` (e cai para `lg` no celular), para o canto sair igual ao dos vizinhos em qualquer navegador; o conteúdo, com a cor do fundo e o raio concêntrico, desenha o resto. Só conquistas usa. Antes o próprio bloco tinha `border` e recorte ao mesmo tempo, o que a regra dos cantos proíbe.
- `Card` (`components/ui/card/`) é a caixa de cada bloco: quem desenha a caixa é o fio do menu (0,6px em `--color-border`), e o cabeçalho, com ícone na cor do título, título e a ação na outra ponta, se separa do corpo por outro fio de borda a borda. O fio de fora não é `border` (decisão de 2026-09-07): a caixa de fora tem a cor do fio e um recuo da espessura dele, e a de dentro (`.inner`) tem a cor do fundo da página com o raio concêntrico, as duas recortadas pelo sistema de cantos (`squircle("xl", { clip: true })` e `squircleAuto({ clip: true })`), para o canto sair em superelipse também onde não há `corner-shape`, igual ao do bloco de conquistas; com `border`, a regra da casa não recorta e o cartão ficava redondo no fallback, o que o usuário viu no Chrome dele ao lado do bloco de conquistas recortado. No fallback o fio tem 1px cheio (`--card-frame`), porque a faixa de 0,6px entre duas caixas se perdia no antialias e sumia em algumas bordas, e a caixa de dentro tem o mesmo raio da de fora, e não o concêntrico: com raio menor a curva de dentro chega mais perto da de fora na quina do que o fio tem de espessura (na diagonal a distância é o recuo vezes raiz de dois menos 0,29 vezes a diferença de raios) e o antialias apaga o fio lá, o que apareceu no iPhone; com o mesmo raio a quina fica com 1,4px e as retas com 1px (o `bare` faz o mesmo; a tentativa de raio menor em duas vezes o fio piorou, pela mesma conta); as duas camadas valem também onde o canto nativo existe (o `@supports` que devolvia a `border` saiu em 2026-09-07): o Safari do iPhone já tem `corner-shape`, e a `border` dele soma nas quinas superelípticas, então o fio de 1px pintado pelas duas caixas é o que aparece igual em todo navegador. A ação é atalho e não foco: `Button` `outline`, `sm` e raio `md`, e fica em 36px também no toque (`--touch-target` rebaixado no invólucro da ação, 2026-09-07), porque com 44px passava do cabeçalho; o alvo grande é o bloco, não o atalho. Raio `xl` com recuo 16 no cabeçalho e no corpo, então o que for aninhado cai no raio 8, exposto em `--card-inner`. No celular o raio cai para `lg` (2026-09-07), no cartão e no bloco de conquistas: com o desenho contínuo do fallback, o de 24px começa a curvar a 38px da quina e lia como pílula nos blocos de uma coluna. O cabeçalho tem a altura do botão `sm` mais o respiro mesmo sem ação, para os títulos dos vizinhos ficarem na mesma linha. Server Component com CSS Module.
- Bloco de projetos (`ProjectsBlock`, 2026-09-04): à esquerda o total ("12 Projetos" em `title3`) com a linha de apoio "Todos os projetos da sua equipe" e, colado na base, as bolinhas de 36px dos três primeiros clientes (no celular entram 12px uma sobre a outra em vez de 8, pela variável `--avatar-overlap` do `AvatarGroup`, e o total encurta por reticências se ainda faltar) com o total de clientes em cor secundária, para pesar menos que o número de cima; à direita o gráfico dos últimos meses. O bloco é duas metades iguais, sempre lado a lado, e nenhuma empurra a outra (pedido de 2026-09-04): quem se adapta é o gráfico, que mede a própria caixa com `ResizeObserver` e mostra só os meses que cabem (32px por mês, no mínimo três, no máximo os que o resumo trouxer), cortando os mais antigos. Sem medida, no servidor e no primeiro quadro, ele não desenha, para nascer no tamanho certo em vez de pular. O gráfico é Recharts com `responsive`, sem eixo, grade ou texto nenhum: duas barras por mês, projetos iniciados no roxo do sistema a 35% e entregues no roxo cheio, porque são a mesma medida em dois tempos e a regra do tema é opacidade para sequência. Roxo, e não o acento azul, por decisão de produto do mesmo dia. As barras andam juntas (`barGap` 2 e `barCategoryGap` 12%), com o topo arredondado. O número de cada mês aparece ao passar o ponteiro ou ao tocar: uma faixa no preenchimento de hover da casa marca o par apontado, e a dica é de vidro, na receita das camadas da casa (fundo a 20% com borrão, fio fino e sombra); ela fica sempre montada com o último mês apontado guardado e só troca de opacidade, com o `visibility` do Recharts forçado visível, então aparece e some com suavidade nas duas direções e o Recharts a move com uma transição curta (2026-09-07), com o mês em `subheadline` semibold e o total do mês na outra ponta, um fio, e uma linha por série, bolinha na cor da barra, nome e o número em `subheadline` (refeita em 2026-09-07: entra crescendo de baixo com a mola curta e sai apagando; enquanto um mês está apontado os outros apagam a 35% por `Cell` com transição, com o mês vindo dos eventos de ponteiro e toque do gráfico), posicionada pelo Recharts e presa dentro da área do gráfico (sem `allowEscapeViewBox`), porque solta ela saía da tela no celular, com o nome do mês saindo da chave `yyyy-MM` no cliente, com `date-fns` em pt-BR. O SVG fica fora da árvore acessível e a leitura por voz é uma lista oculta ao lado. Em bloco estreito o gráfico desce para baixo do resumo. O bloco inteiro virou gatilho de janela em 2026-09-08, a pedido: a dica do gráfico é hover, e hover não existe no toque, então no celular o número de cada mês não tinha como aparecer. Agora tocar ou clicar em qualquer ponto do bloco abre `ProjectsSheet` (`features/projects/components/`), a ficha com os números por extenso: quatro azulejos (projetos, clientes, iniciados e entregues no período), a fila de clientes atendidos e, depois de um fio, mês a mês com as duas séries em barra e o número de cada uma, na mesma dupla de roxos do gráfico. As barras são proporcionais ao maior mês do período, e não a cada linha, senão um mês fraco encheria a barra igual a um mês forte e a comparação entre linhas se perderia. A dica do gráfico passou a existir só sob `(hover: hover)`, a mesma regra do hover da casa, e a lista oculta que ficava ao lado do gráfico saiu: dentro de um `role="button"` ela não é lida, e quem carrega os números para o leitor de tela agora é a ficha. O resumo chega por prop (`ProjectsSummary`, em `features/projects/summary.ts`) e hoje vem de `features/projects/preview.ts`: **o domínio não existe no banco**, e quando existir muda só a origem.
- Bloco de financeiro (`FinanceBlock`, 2026-09-05): visão do caixa, e não cartão de verdade, por decisão de produto a partir de uma referência de carteira digital. Em cima, o caixa vestido de cartão: grade 2x2 com o ícone da marca (`Logo` em `icon`, 24px) no alto à esquerda, a situação em `Badge` verde com ícone ("Ativo") à direita, "Em caixa" em `footnote` secundária com o saldo em `title1` semibold embaixo à esquerda (sem algarismo tabular e maior que o `title2` da primeira versão, por pedido de presença; `formatMoney` em `lib/utils/format.ts`, por extenso a partir de centavos, ao lado do `compactMoney` do menu), e um botão de atualizar no canto de baixo à direita (`RefreshButton`, Client Component pequeno em `features/dashboard/components/`, `IconButton` fantasma `sm` que roda `router.refresh()` numa transição, mostra só o giro enquanto dura e, ao terminar, dispara o toast "Financeiro atualizado"; o `Button` só de ícone deixou de mostrar giro e glifo lado a lado em `loading`, defeito corrigido em 2026-09-07). O cartão tem a largura do corpo, declarada em 100% e não automática (com proporção e teto de altura, largura automática é recalculada a partir da altura quando o teto entra, e no celular ele encolhia em vez de tomar a coluna; acerto de 2026-09-07), e a altura de cartão de verdade, saindo da largura: 7 por 4 com piso de 8,5rem e teto de 11rem no desktop, e 8 por 5 com piso de 9,5rem e teto de 13,5rem no celular, que é a proporção do cartão físico e foi pedida em 2026-09-08 para o bloco ter mais cara de cartão (o 7 por 4 com teto de 12,5rem lia como faixa na coluna larga do celular, onde o bloco tem duas linhas) (pedido do mesmo dia; a versão que sangrava 8px para os lados saiu, porque o pedido era altura e não largura), e a linha de baixo toma a sobra com o conteúdo assentado na base. Fundo `--color-fill-quaternary`, o das caixas do menu, raio `lg` por `squircle("lg")`, e não o concêntrico de 8px que a regra dos cantos daria (pedido de 2026-09-05: com o recuo de 16 do bloco a conta dava um canto que lia como quadrado num cartão desse tamanho), com recuo interno de 16; no fallback o motor recorta, porque o cartão já esconde o que transborda, e o anel de foco do botão fica 16px para dentro e não é cortado. O desenho do cartão é o `public/bg/card-bg.svg` do usuário, dois traços que descem em curva, usado como máscara CSS num `::before` e pintado com a cor do texto a 22%, a técnica do `Logo`, então a cor sai de token e segue o tema em vez do preto do arquivo; fica só no canto de cima à direita, com uns três quartos da altura do cartão (a primeira versão passava da altura inteira e espalhava demais), e o que passa da borda é cortado pelo `overflow`. Antes dele passaram por ali dois fios em `--color-border` e depois gradientes radiais concêntricos, os dois descartados no mesmo dia a pedido. A pasta `bg` já estava no `matcher` do proxy, então a máscara carrega sem redirecionamento. Embaixo, "Movimentações recentes" em `caption1` e a lista, com no máximo cinco (`SHOWN_TRANSACTIONS`): quem está do outro lado em 36px no raio de metade do lado (`--icon-button-radius-sm`, a escala do botão de ícone e do `Avatar` em squircle), título em `subheadline` medium e descrição em `footnote` secundária, os dois com reticências, e na outra ponta o valor em semibold tabular, sempre em cor neutra seja qual for o tipo (decisão de 2026-09-05), e a data em `caption1` no formato "2 set. 2026", com o ano sempre à vista. Quem está do outro lado sai de `visual` (`TransactionVisual`): pessoa é o `Avatar` em `squircle` com a foto ou o rosto gerado; serviço é a logo em cores pelo `BrandIcon` de `public/brands` sobre `--color-fill-tertiary`, a base do próprio `Avatar`, sem borda para o fallback recortar na superelipse (`squirclePx(18, { clip: true })`); sem nenhum dos dois, o ícone do tipo num chip igual que define só `--chip-hue`, na receita do Badge (tinta a 70% do rótulo, fundo em alfa de 12% no claro e 18% no escuro). Os tipos mudam só o ícone de reserva, o sinal e a data: recebido em verde com sinal de mais, saída em neutro com sinal de menos, e por vir no acento, sem sinal e com "Previsto" antes da data, com `date-fns` em pt-BR. O cartão fica fixo no topo e a lista toma o que sobra e rola por dentro no desktop, onde a altura do bloco é pré-definida. A lista é a de `block-list.module.css` (2026-09-07), compartilhada com o bloco de clientes: título pequeno mais linhas em grade de três colunas (quem está do outro lado, texto, o que for da linha na ponta), sangrando 8px do recuo para o hover pintar uma caixa em volta da linha, no ritmo do menu, e desbotando nas pontas por `mask-image` na receita das listas das camadas do menu (faixa curta em cima, maior embaixo), com respiro no fim para a última linha aparecer inteira no fundo da rolagem. O que é só do financeiro (chip por tipo, valor e data) fica no módulo dele. Server Component com CSS Module, e o único JS é o botão de atualizar. O resumo chega por prop (`FinanceSummary`, em `features/finance/summary.ts`, valores em centavos sempre positivos com o sinal saindo do tipo) e hoje vem de `features/finance/preview.ts`, com datas relativas a hoje e pessoas sem foto para o rosto gerado aparecer: **o domínio não existe no banco**.
- Bloco de clientes (`ClientsBlock`, 2026-09-07): quem chegou por último em destaque e, embaixo, os outros mais novos. O destaque é a foto à esquerda e, à direita, tudo o mais empilhado em `flex` (decisão de 2026-09-07, depois de uma versão em grade com a ficha de ponta a ponta): a foto é o `Avatar` em `squircle` com largura fixa de 5,5rem, 7rem no celular (pedido de 2026-09-08: na coluna larga do celular a ficha é alta e o retrato saía fino e comprido; quem cede é o texto ao lado, que já encurta por reticências, e os fatos empilham numa coluna só), e altura esticada à da pilha ao lado (`align-items: stretch`, pedido de 2026-09-07: a foto acompanha o conteúdo, e não o contrário), então sai em pé, com a imagem cobrindo e o rosto gerado centrado no pastel, e `--avatar-radius` trocado para `lg` por três classes (é retrato, não bolinha de lista; o motor lê o raio do `border-radius`, então o squircle segue), e a pilha da direita, com 8px entre as partes, começa pela linha do nome, em `title3` semibold, seguido da etiqueta "Novo" (`Badge` `accent` `sm`, que não encolhe: quem cede é o nome, por reticências; era "Novo cliente" até 2026-09-08, quando encurtou a pedido, aqui e no perfil, para devolver largura ao nome no celular) e com o menu de mais opções na outra ponta (`flex` com `space-between`, centrados); segue a ficha em `dl`, uma grade de fatos com rótulo em `caption1` e valor em `footnote` medium (e-mail e telefone, "Não informado" quando falta), em quantas colunas de 7rem caibam (uma só na coluna do desktop, onde a foto toma um terço), sobre o fundo do botão secundário com raio `--card-inner`, e acaba aí. O menu de opções é o `ClientMenu` (`features/clients/components/`, sobre o `DropdownMenu` que saiu do stub em 2026-09-07), com o chevron duplo do menu no lugar dos três pontos como gatilho (a versão com fundo durou uma rodada e saiu a pedido). Passaram pelo destaque e saíram no mesmo dia, a pedido: a linha "Cliente desde" com a data de entrada e o botão "Fazer orçamento" que levava a `/orcamentos/novo?cliente=<id>`. Os ícones vão para os botões já com `weight="bold"`: o botão é Client Component e o que chega lá do servidor é o SVG pronto, que o `matchIconWeight` não consegue mais vestir. Embaixo, "Outros clientes" na lista de `block-list` com até cinco (`SHOWN_CLIENTS`): `Avatar` `sm` em squircle, nome e telefone, e o mesmo menu de opções do cliente na ponta. Telefone vai no resumo só em dígitos, como no banco, e ganha máscara na tela por `applyPattern("phone")` de `lib/masks.ts`, função nova que aplica o padrão de um campo fora do campo. Sem cliente nenhum, uma frase em `footnote` no lugar do bloco (`EmptyState` segue stub). Server Component com CSS Module. O resumo chega por prop (`ClientsSummary`, em `features/clients/summary.ts`, do mais novo para o mais antigo) e hoje vem de `features/clients/preview.ts`, todo mundo sem foto para o rosto gerado aparecer: **o domínio não existe no banco**.
- Bloco de tarefas (`TasksBlock`, 2026-09-07): uma linha de abertura em `caption1` com "Tarefas pendentes" de um lado e a data de hoje ("7 set. 2026") na outra ponta, a 12px da lista para não brigar com o primeiro cartão, e depois as tarefas mais próximas do vencimento, até cinco (`SHOWN_TASKS`), da mais urgente para a mais distante, seguindo a referência de "notas" do usuário sem a fila de filtros. Cada tarefa é um cartão no fundo do botão secundário com raio `md` por `squircle("md", { clip: true })` (objeto solto na lista, e não linha, então fora da conta concêntrica do bloco), com 12 de recuo e 8 entre cartões: em cima o título em `headline` (subiu de `subheadline` no mesmo dia, a pedido de presença) com duas etiquetas `sm` ao lado, o prazo e a situação, e o chevron (`IconButton` fantasma com `CaretRight`, de pé e mudo porque a tela de tarefas ainda é só rota vazia) na outra ponta, alinhado pelo topo; as etiquetas andam com o título e quebram para a linha de baixo quando não cabem, em vez de espremer o nome. No meio a descrição em `footnote` secundária, cortada em duas linhas por `line-clamp`. Embaixo as bolinhas de até três pessoas (`AvatarGroup` com `Avatar` `xs`, 24px, entrando 6px uma sobre a outra pela variável do grupo) e "Com Miguel, Jhon e Hane" em `footnote`, "Com" secundário e os primeiros nomes em peso medium, ligados por `Intl.ListFormat` em pt-BR. O prazo em uma palavra quando dá: "Hoje" em `danger`, "Amanhã" em `warning`, "Ontem" ou a data curta ("9 set.") em neutro; tarefa aberta com prazo vencido também fica em `danger`, porque é a mais urgente da lista, e concluída no passado fica neutra. A situação em três tons: "A começar" em `success`, "Em andamento" em `accent` e "Concluída" em neutro, no mapa da referência (Incoming, Ongoing, Past). A lista é a de `block-list` pela máscara nas pontas e pela rolagem, com a sangria do hover zerada e vão entre cartões por duas classes. Server Component com CSS Module. O resumo chega por prop (`TasksSummary`, em `features/tasks/summary.ts`: título, descrição, prazo, situação e pessoas envolvidas) e hoje vem de `features/tasks/preview.ts`, com datas relativas a hoje: **o domínio não existe no banco**.
- Sequência completa (`StreakSheet`, em `features/gamification/components/`, 2026-09-07, sobre uma referência de tela de streak do usuário): o bloco de desafio diário ganhou no cabeçalho o atalho "Ver sequência" (`StreakButton`, Client Component, no mesmo contorno pequeno dos atalhos de rota; a grade aceita um atalho próprio por bloco, que ganha do atalho de rota do registro), que abre a janela `md` (`DetailsDialog`, a janela de conteúdo próprio que saiu do gatilho de detalhes para peça avulsa: vidro, X flutuante e rolagem por dentro; o azulejo da chama como gatilho durou uma rodada e voltou a ser só o azulejo, a pedido) com a sequência inteira: em cima a semana em curso com uma marca por dia (a chama da casa em 28px no dia com a meta batida, ponto de exclamação em laranja no dia abaixo da meta, X em vermelho no dia sem acesso e círculo tracejado com o número nos que ainda vêm); no meio a chama em 6rem com sombra laranja e os dias seguidos em 4rem ("142", "dias seguidos na plataforma"), entrando com a mola curta; e embaixo o calendário num cartão no preenchimento da casa, com setas para andar mês a mês entre o mês do primeiro acesso e o atual, o mês por extenso, os dias da semana em uma letra e a grade de sete colunas com o número do dia e um ponto embaixo (verde meta batida, laranja abaixo da meta, vermelho sem acesso; nada antes do primeiro acesso e nos dias por vir, que ficam apagados), hoje na pílula da cor do texto, e a legenda dos três pontos. Client Component em Emotion pela troca de mês. O modelo ganhou `history`, um dia por dia desde o primeiro acesso, e a prévia gera os 142 dias por um padrão determinístico, com a semana em curso lida do mesmo histórico para os dois lugares baterem.
- Comemoração dos pontos (`ClaimReward`, 2026-09-07, sobre uma referência de prêmio do usuário): o arrasto do bônus diário (`ClaimSlider`) passou a vir embrulhado num Client Component que, ao chegar no fim, abre a janela `sm` de vidro da casa com só os pontos ganhos em número grande ("+50", 4rem, entrando com a mola curta; a estrela da gamificação durou uma rodada e saiu a pedido), dezesseis papéis de confete nas cores da paleta do sistema saindo do centro uma vez (trajetórias fixas em variáveis por papel, para a explosão ser sempre a mesma; somem em movimento reduzido), "Pontos recebidos!" com uma frase e o botão "Continuar". O bloco de conquistas usa o embrulho no lugar do arrasto solto.
- Bloco de equipe (`TeamBlock`, 2026-09-07): uma linha de altura, e por isso duas linhas de conteúdo a 12px uma da outra: em cima a fila de fotos, que toma toda a altura que sobra, e embaixo a ficha de quem está em foco. Em cima, a fila de fotos (`Avatar` com `--avatar-size: 100%` por `style`, então a foto tem a altura da fila, quadrada, entre um piso de 32px e um teto de 6rem: pedido de 2026-09-07 para a foto ocupar o espaço em branco em vez de ter um número fixo; no corpo mínimo de 108px fica no piso, e a conta fecha por um fio com a ficha em entrelinha justa) que rola na horizontal com encaixe, com a barra da casa embaixo, a 8px das fotos, e sem máscara nas pontas (numa fila curta ela apagava a primeira foto com a fila ainda no início); com o mouse a fila também anda arrastando, cursor de mão, ouvintes na janela em vez de captura de ponteiro (a captura mudaria o alvo do clique seguinte) e o encaixe desligado durante o arrasto, para a fila seguir o ponteiro e só encaixar ao soltar; um arrasto de mais de 4px engole o clique que viria na foto onde o ponteiro soltou. As setas de rolar, que passaram por fora e depois por cima da fila sobre um degradê, saíram a pedido no mesmo dia; cada foto é um botão redondo do tamanho exato da foto (`inline-flex` e altura de linha zero, porque a altura de linha do botão deixava o anel oval; no toque o piso sobe para o alvo de 44px, para escolher com o dedo), com o ponto de situação na quina em 22% da foto (verde para quem já está, laranja para convite em aberto); em foco, anel na cor da marca separado da foto por um vão na cor do fundo, o mesmo par do dia selecionado no calendário, e as outras fotos ficam a 72% de opacidade, voltando ao cheio no hover. A fila tem 4px de respiro devolvidos pela margem, para o anel não ser cortado pela caixa que rola, e é posicionada para medir o `offsetLeft` das fotos ao trazer a escolhida para a vista com `scrollTo` suave. Embaixo, a ficha de quem está em foco numa faixa com o preenchimento do botão secundário, recuo de 8 por 16 e raio `--card-inner` (voltou a pedido; cabe porque agora é a fila de fotos que cede altura): nome em `subheadline` semibold e função em `caption1` secundária (ou "Convite pendente", que vale mais que a função para quem ainda não entrou), encurtando por reticências, e na outra ponta duas medidas em `Badge` `soft` `sm` neutro com ícone e valor, iguais entre si por pedido de padronização (2026-09-07; antes eram três, orçamentos aprovados, projetos entregues e tarefas concluídas, cada uma com o tom do próprio domínio): projetos entregues e faturamento, este em centavos no resumo e encurtado na tela por `compactMoney`, o mesmo do menu; o nome da medida vai para o leitor de tela (`dt` oculto) e para a dica do ponteiro (`title`). Trocar a pessoa não mexe na linha: só os textos (nome, função e os três números) remontam por `key` e entram com um fade curto, cortado em movimento reduzido, então nada salta (pedido de 2026-09-07). Client Component em Emotion, porque tem estado: o foco começa na primeira pessoa. Passaram por aqui e saíram no mesmo dia, a pedido: cartões de 9rem por pessoa com "Perfil" e as medidas numa fila abaixo da faixa, e depois o cartão em foco abrindo em ficha larga ao lado da foto, que na coluna do desktop tomava o corpo inteiro e escondia os outros. O `Carousel` de `components/ui` não serviu: é slideshow de imagens. O resumo chega por prop (`TeamSummary`, em `features/organizations/summary.ts`: nome, função, foto, situação e as duas medidas) e hoje vem de `features/organizations/preview.ts`: **os membros existem no banco, as medidas por pessoa não**, e nascem com projetos e cobranças.
- Bloco de desafio diário (`ChallengeBlock`, 2026-09-07; chamou-se "Desafio semanal" até o fim do dia, quando o nome mudou a pedido porque a meta é por dia): entrou na grade logo abaixo de equipe, com uma linha de altura, ícone de alvo e sem atalho, porque não há tela. É a gamificação do uso, refeita no fim do dia sobre uma segunda referência do usuário (um widget de sequência de passos): à esquerda o azulejo da sequência, com piso de 4,5rem e a largura do próprio texto (3.000 dias, com separador de milhar por `Intl.NumberFormat`, alargam o azulejo em vez de quebrar) e o preenchimento do botão secundário, só com a chama, a ilustração do usuário em `public/bg/fire-streak.png` por `next/image` com 48px de altura (trocou o ícone `Fire` num círculo laranja, a pedido), e "142 dias" em `subheadline` semibold embaixo dela, com "seguidos na plataforma" completando a frase só para o leitor de tela; passaram por aqui e saíram no mesmo dia o número grande com o rótulo "dias seguidos" e, depois, uma bolha de vidro fixa do `Tooltip` por cima da chama; à direita, o que já foi de hoje em `title1` com entrelinha 1 e 2px de respiro nos lados (o tracking negativo do título empurra o primeiro algarismo para fora da caixa) e "/ 30 min hoje" em `callout` apagado na mesma linha de base, com "Desde 19 abr. 2026" (o primeiro acesso, `since`) em `caption1` na outra ponta da linha, encurtando antes do número, a barra da casa (`Progress` `lg`, 16px, no roxo do painel por `--tone` sobrescrito, com um segmento por dia do mês em curso, `getDaysInMonth`, para a barra ler como o calendário do mês, e os segmentos com canto de 2px em vez da pílula, por `--tick-radius` sobrescrito com três classes; subiram de `title2` e `sm` a pedido, para aproveitar o container) e, embaixo, a semana numa faixa com o mesmo preenchimento: sete colunas iguais com uma bolinha de 28px em cima (subiu de 20 a pedido, para a semana ocupar o espaço em vez de sobrar vão; a faixa tem recuo de 8 em volta) (vazia no preenchimento terciário; com a meta de 30 minutos batida, o check na cor da barra sobre a mesma cor em alfa baixo, na receita do Badge, com um fio da mesma cor em volta para destacar; hoje sem a meta, anel vermelho), a 4px do dia e "Seg" a "Dom" em `caption2` embaixo. No celular tudo um degrau menor (número em `title3`, meta em `footnote`, barra média, bolinhas de 24px, chama de 40px), para a coluna da meta caber ao lado do azulejo sem apertar. Três matizes chapados, sem degradê, na pegada da chama (pedido de 2026-09-07): a barra e o check no roxo do painel (`--challenge-bar`, com `--challenge-check` apontando para ela: o check em laranja durou uma rodada, porque check e barra dizem a mesma coisa) e o dia de hoje no vermelho (`--challenge-today`); a versão de um laranja só também durou uma rodada. A sequência (`streakDays`) conta os dias seguidos em que a pessoa entrou na plataforma, o pedido central desta versão. Acessos e tempo de cada dia saíram da tela e vivem na leitura por voz (uma frase por dia, oculta, com a data por extenso) e na dica do ponteiro. Passou por aqui e saiu no mesmo dia a primeira versão, com o dia do mês nas bolinhas, anéis de progresso, o tempo embaixo de cada dia e a faixa amarela da posição entre os usuários, que também deixou o modelo. Server Component com CSS Module. O resumo chega por prop (`WeeklyChallenge`, em `features/gamification/summary.ts`: a sequência, o primeiro acesso, os sete dias e a meta diária) e hoje vem de `features/gamification/preview.ts`, que monta a semana em curso relativa a hoje: **o domínio não existe no banco**, e nem a sequência nem a contagem de acessos e tempo online são medidas em lugar nenhum; vão precisar de um registro de sessão por dia.
- Bloco de último orçamento (`QuoteBlock`, 2026-09-07): o último orçamento que a pessoa criou, em ficha na pegada da referência de projeto do usuário, adaptada a orçamento. É um `dl` com cinco linhas e a sobra dividida entre elas (`space-between`), então em linha alta se espalham e em linha baixa ficam a 8px; no celular, onde o corpo de duas linhas tem 332px e cinco linhas de 36px com rótulo passavam disso, chips e bolinhas caem para 24px, todo texto desce dois degraus (rótulos em `caption2`, título e valores em `footnote`, o dinheiro em `subheadline`) e os campos dividem a sobra entre si com 12 de vão mínimo, então a ficha ocupa o bloco inteiro (empilhar do topo deixava um vazio embaixo); cada campo tem o rótulo em `caption1` secundária em cima e o valor a 4px, como na referência, e os campos curtos andam em pares de metades iguais para aproveitar a largura (refeito no mesmo dia, a pedido: a primeira versão era miúda e usava o ponto como separador no prazo, o que a regra proíbe). As linhas: o identificador ("ORC-2026-0042", no padrão de `lib/utils/reference.ts`) como rótulo, com o chip de recibo, o título em `headline` como `TextLink` herdando a cor para `/orcamentos/<id>` e a etiqueta de situação `sm` com ícone ao lado (rascunho neutro, enviado no acento com relógio, visualizado em `info` com olho, aprovado em `success` com check, recusado em `danger` com X, vencido em `warning` com ampulheta), que desce para a linha de baixo em coluna estreita em vez de espremer o título; "Cliente" e "Responsável", cada um com `Avatar` `sm` e nome; "Valor" (chip de moeda, o valor em `title3` semibold e a contagem de itens colada embaixo em `caption1`) e "Pagamento" (chip de cartão e "3x de R$ 6.133,33", ou "À vista"); "Enviado em" e "Válido até", cada data com o próprio chip de calendário ("Ainda não enviado" e "Sem prazo" quando faltam); e "Descrição", com o chip de lápis e o texto em cor secundária em até duas linhas. Chips e bolinhas têm 36px e o raio de metade do lado, para todos os campos alinharem pela mesma coluna. Sem orçamento nenhum, a frase e o convite "Criar orçamento" (`Button` `outline` `sm`) centrados no bloco. Nenhum número em algarismo tabular, a pedido: tudo na Inter padrão. Server Component com CSS Module. O resumo chega por prop (`QuotesSummary`, em `features/quotes/summary.ts`: identificador, título, cliente, responsável, valor em centavos, parcelas, itens, situação, datas e descrição) e hoje vem de `features/quotes/preview.ts`, com datas relativas a hoje: **o domínio não existe no banco**.
- Bloco de conquistas (`AchievementsBlock`, 2026-09-07): o único sem o cartão padrão, sem cabeçalho, atalho nem fio, por decisão de produto: é a cara da gamificação no painel, na pegada de pontos, sobre uma referência do usuário. O bloco se declara `bare` em `blocks.ts` e a grade o põe direto na área, num invólucro que desenha o fio e o raio como o `Card`; o bloco traz a cor do fundo, o raio concêntrico e o recuo de 16 direto nele. O fundo é a cor genérica da casa com o fio dos outros cartões e, por trás do conteúdo, as quatro bolas de cor sangrando pelas quinas, a receita do container do plano nos primeiros passos (índigo e menta de um lado, vermelho e amarelo do outro, gradiente radial em vez de filtro de desfoque), na escala do bloco e a 45% de opacidade para o texto continuar em contraste (decisão de 2026-09-07: um degradê próprio nas cores do adesivo, forte e depois pastel com `light-dark()`, existiu por umas horas e saiu porque destoava do resto). A tinta e o vidro do controle de arrastar são `--gamification-ink` e `--gamification-pill`, que agora apontam para `--color-label` e `--color-fill-quaternary`. Dois containers empilhados com a sobra entre eles: em cima, meio a meio, o adesivo à esquerda (quadrado entre 64 e 96px, com sombra leve) e à direita os números, o total em `title1` semibold com "pontos" menor e apagado na linha de base ("1.590", separador de milhar por `Intl.NumberFormat`) e os dois fatos numa grade de duas colunas, rótulo curto apagado em cima e valor em `subheadline` semibold embaixo ("Ranking #3.329" e "Por dia 30 pts"); embaixo, de ponta a ponta, o `ClaimSlider` (`features/dashboard/components/claim-slider.tsx`, Client Component em Emotion porque tem estado): uma pílula no preenchimento da casa da altura do controle médio com "Arraste para ganhar 50 pontos" e uma bolinha de 36px na cor do texto (preta no claro, branca no escuro) com a seta dupla na cor do fundo, que segue o ponteiro por captura (mouse e dedo) com a pista se preenchendo atrás dela num degradê de alfa da cor do texto, mais claro no começo e mais cheio no fim, pintado na pista inteira e revelado por `clip-path` até a borda da bolinha (pedido de 2026-09-07: preto e branco, limpo; antes passaram por ali o roxo do painel e um degradê de várias cores, cada um por uma rodada), volta com a mola curta da casa se solta antes de 85%, preenchimento junto, e completa e trava passando disso, com a pista inteira preenchida, virando check com "50 pontos ganhos hoje"; no teclado, Enter, Espaço, End ou a seta para a direita completam direto; o texto apaga conforme a bolinha avança. A posição vai direto para as variáveis do nó, e não para estado do React, escrita uma vez por quadro (acerto de fluidez de 2026-09-08): um `setState` por evento de ponteiro remontava a pista, o preenchimento, o rótulo e a bolinha dezenas de vezes por segundo, e o arrasto saía travado. A bolinha declara `touch-action: none` e ganha `will-change: translate` enquanto anda; a pista segue em `pan-y`, então rolar a página tocando fora dela continua funcionando. Vale uma vez por dia: `dailyBonus.claimedToday` no resumo faz o controle nascer travado no fim. O bloco em si é Server Component com CSS Module. O resumo chega por prop (`PointsSummary`, em `features/gamification/summary.ts`: pontos, posição, geração diária e o bônus do dia) e hoje vem de `features/gamification/preview.ts`: **o domínio não existe no banco**, nem a pontuação nem o ranking são calculados em lugar nenhum, e o arrasto ainda não grava nada, só muda o estado na tela; quando for para valer, `onClaim` chama a action que credita os pontos e marca o dia. Antes disso o bloco ficou vazio de 2026-09-05 a 2026-09-07, depois de perder no mesmo dia uma trilha de níveis em hexágonos; do que passou por ali ficaram na casa o `ProgressRing` (`ring` e `gauge`), a máscara `--shape-hexagon` com o `Avatar` em `shape="hexagon"` e o `underline="dashed"` do `TextLink`.

### Topo e barra de página

O topo padrão de toda página da aplicação, menos o painel, que abre com a pessoa e não se enquadra (2026-09-08, sobre uma referência do usuário).

- **`Topbar`** (`components/layout/topbar/`) tem duas partes e mais nada: o nome da página à esquerda, como `h1` em `headline` semibold, e o uso da IA na ponta direita. A trilha até a página (área, pasta e página, sobre uma referência de breadcrumb) e o nome preso no meio existiram cada um por uma rodada em 2026-09-08 e saíram a pedido: o menu lateral já diz onde a página mora, e o topo ficou só com o que é dele. O nome sai de `navLocation` (`components/layout/nav.ts`), que acha a rota no menu, a fonte única das rotas; a página troca por `title` quando se chama diferente do menu, como a base de clientes, que no menu é "Base de clientes" e no topo é "Clientes". `navLocation` e `isCurrent` seguem em `nav.ts`, servindo à busca e ao menu.
- **O caminho e o nome saem de `navLocation`** (`components/layout/nav.ts`), que acha no menu onde a rota está. Vem de lá, e não de um mapa novo, porque `navGroups` já é a fonte única das rotas: página que nasce no menu ganha o topo de graça e não há um segundo mapa para sair de sincronia. Entre duas rotas que casam ganha a mais específica, senão `/orcamentos/novo` leria como "Acompanhar", que também casa por prefixo. O `isCurrent` que o menu já usava subiu para `nav.ts` e agora serve aos dois.
- **O caminho é uma trilha, e não um menu** (a pedido, sobre uma referência de breadcrumb): os degraus separados por barra, cada um com o glifo do menu na frente, em `footnote` medium. **O degrau da pasta leva o glifo num azulejo cheio no matiz dela**, com o traço em branco, e o da área sai solto e apagado: é o contraste da referência entre o contexto largo e o lugar onde a página mora de verdade. O matiz é o mesmo que a busca já usa para tingir as páginas daquela pasta, então a cor de cada seção é a mesma nos dois lugares. O último degrau é o mais escuro, porque é o mais perto da página. Não é link nem dropdown: ele diz onde a pessoa está, e quem leva a outro lugar é o menu lateral. Marcação de `nav` com `ol`, porque é isso que uma trilha é e o leitor de tela anuncia a ordem dos degraus. Para isso o grupo do menu ganhou `icon` em `nav.ts`, que antes só as pastas tinham.
- O nome é o `h1`, então a regra de um `h1` por página se resolve no próprio topo, sem título oculto. Ele cede e encurta por reticências; o widget não encolhe.
- **O widget da IA** é a marca da OpenAI (`BrandIcon` em máscara, na tinta do texto; o `font-size` do link é o que a dimensiona, porque o `BrandIcon` mede em `em`) e uma régua de dez barras finas em pílula, sem caixa nenhuma em volta: soltas na moldura, no fantasma dos controles da casa, na altura do controle pequeno e com preenchimento só no hover (a caixa com fio e fundo durou uma rodada e saiu a pedido de algo menor e mais minimalista). Um degrau por décimo do plano, os acesos na tinta do texto e os apagados em `--color-fill-secondary`, que sobre o preto ainda aparece. Monocromático de propósito, porque mora na moldura e não é etiqueta de estado. Sem número na tela; ele fica na leitura por voz e na dica do ponteiro. Qualquer uso acende ao menos um degrau, senão a régua mentiria com o plano recém-começado. O uso chega por prop (`AiUsage`, em `features/ai/summary.ts`) e hoje vem de `features/ai/preview.ts`: **o domínio não existe no banco**.
- **`PageToolbar`** (`components/layout/page-toolbar/`) é a barra logo abaixo, refeita em 2026-09-08 sobre uma referência do usuário e acertada no mesmo dia para ler como uma peça só. **A barra é a dona da busca**: recebe `search` como valor, troca, placeholder e nome, e desenha o campo na receita exata do campo de busca do menu lateral (fio de 0,6px em `--color-border`, raio `md`, fundo transparente que ganha o preenchimento da casa no hover e no foco, lupa de 1rem, fonte `subheadline` em secundário, com o `input` cru por dentro e a tecla `/` num `Kbd` na ponta). A tecla leva o foco ao campo de qualquer lugar da página, salvo quando a pessoa já digita em outro campo, e some no celular junto com o `Kbd`. A busca toma toda a largura que sobra, e o campo não leva anel de foco, na mesma decisão dos campos da casa. **Colados a ela, a 4px, vêm os menus**: `quickFilters` são os que ficam à vista (ordem e período, que a pessoa troca toda hora), cada um a configuração de um `DropdownMenu` só com o ícone, e `filters` são as seções do menu que abre no funil; a barra desenha o funil e a contagem dos que saíram do padrão na quina, em `activeFilters`. **A barra é quem veste os botões** (`FIELD_TRIGGER`, acerto de 2026-09-08 a pedido): variante secundária, para o botão se ver, com o canto `md` pelo sistema de cantos e o glifo na mesma tinta secundária da lupa; o fio sai na espessura do fio do campo por `--button-line`, variável que o `Button` lê para a borda (1px por padrão) e que a barra declara em 0,6px. Por isso os menus rápidos chegam como dado, e não como `ReactNode`: a receita mora num lugar só. Todos abrem o menu de vidro do chevron duplo das listas, pelo `trigger` do `DropdownMenu`, que é o `Pick` de variante, canto e cores do `Button`. Um `Listbox` vestido de botão durou uma rodada e saiu: o gatilho dele é outra família e o painel não é o de vidro. A ação principal da página fecha a linha, um passo afastada. Uma linha só em qualquer largura, toda peça na altura do controle pequeno (e no alvo de toque em ponteiro grosso, campo incluído). **No celular sobram três peças** (a pedido, 2026-09-08): a busca, o funil e a ação. Os menus rápidos somem da linha por CSS e as seções deles entram no começo do funil, que ali é a bandeja de vidro, com as escolhas em `keepOpen` como os outros filtros; a etiqueta do funil soma `activeQuickFilters`, que a página informa, porque ali dentro está tudo. O conteúdo do menu só monta quando abre, depois da hidratação, então ler a largura por `useMediaQuery` para juntar as seções não faz servidor e cliente discordarem. A tecla some junto com o `Kbd`.
- O que age sobre a seleção só aparece no celular quando há seleção: sem ela seria um botão desligado roubando espaço da busca. No desktop ele fica sempre, desligado, porque lá sobra largura.

### Clientes

A base de clientes em grade de cartões, sobre uma referência do usuário (2026-09-08). É só a listagem: o cartão mostra quem é a pessoa e o resto sai pelo leque de ações.

- **O cartão** (`ClientCard`, `features/clients/components/`) tem em cima a caixa de seleção à esquerda e o chevron duplo do `ClientMenu` à direita, e embaixo a foto (4,5rem no celular, 5,5rem no desktop), o nome em `subheadline` semibold e a empresa em `caption1` terciária, tudo centrado e encurtando por reticências. O cartão inteiro abre a gaveta lateral com a ficha; marcar a seleção ou abrir o leque não abre nada, porque cada um tem ação própria, e o anel de foco é de teclado, então marcar com o dedo ou o mouse não acende anel nenhum. A caixa usa o fio em duas camadas do `Card`, com o mesmo desvio do Safari do iPhone, e o canto cai para `lg` no celular. Selecionado, quem acende é o fio, na cor de destaque: mudar o fundo faria o cartão inteiro piscar a cada marcação numa grade cheia. A gaveta é uma só para a tela (`ClientDrawer`), e não uma por cartão.
- **A grade** enche a largura com quantos cartões couberem, com piso de 8,5rem no celular, o maior que ainda dá duas colunas em 320px, e 10,5rem daí para cima, porque no desktop o cartão estava miúdo para o retrato que carrega. `auto-fill`, e não `auto-fit`, para que com poucos clientes os cartões fiquem do tamanho dos outros em vez de esticarem para ocupar a linha.
- **A barra** é o `PageToolbar` da casa: a busca ("Buscar clientes", por nome, empresa, e-mail ou telefone), dois menus rápidos só no ícone (ordem com as setas, período com o calendário, cada um com o escolhido marcado e no nome acessível do gatilho, fechando ao escolher como o período do painel), o funil com os demais filtros (Favoritos e Situação em escolha única, Com e-mail e Com telefone em interruptor, e Limpar filtros, que só aparece com algo em vigor e devolve tudo ao padrão, ordem e período inclusive, porque no celular eles moram dentro deste mesmo menu), e "Novo cliente" na ponta. No funil cada escolha vale na hora e não o fecha (`keepOpen`), porque a pessoa costuma ajustar mais de uma antes de sair. Situação e contato entraram na URL em 2026-09-08 (`situacao`, `email=1`, `telefone=1`), lidos por zod como os outros, e `countMenuFilters` e `countQuickFilters` em `list-options.ts` contam o que saiu do padrão em cada grupo; `clearedFilters` é o que o limpar aplica. Excluir e exportar sobre a seleção saíram da barra no mesmo dia, a pedido; a marcação dos cartões fica, para as ações que virão.
- **O filtro vive na URL** e quem trabalha é o servidor (`listClients` em `features/clients/list.ts`, com zod lendo os parâmetros, valor inválido caindo no padrão), então a página é compartilhável e volta igual pelo histórico. Trocar qualquer filtro leva de volta para a primeira página, senão a pessoa cairia numa página que o novo filtro não tem. O campo de busca espera 320ms a pessoa parar de digitar, senão seria uma ida ao servidor por letra. A busca compara por `slugify` dos dois lados, e o telefone só em dígitos, porque na tela ele tem máscara e no banco não.
- **O cartão carrega só o que mostra** (`ClientListItem` em `list-options.ts`, um `Pick` da ficha): a ficha completa tem orçamentos, projetos, anotações e etiquetas, e mandar tudo isso para doze cartões encheria a carga com o que a tela nem desenha. O `ClientMenu` teve a prop estreitada para o `Pick` que ele usa, então a ficha inteira continua servindo onde já servia. Quando a tabela existir, a consulta seleciona estas colunas e mais nenhuma.
- As partes leves (modelo, nomes dos parâmetros e listas dos menus) ficam em `list-options.ts`, e o zod em `list.ts`, pela mesma regra do painel: a barra é componente de cliente e importar do módulo com zod levaria a biblioteca para o navegador.
- O topo é o `Topbar` padrão, que traz o `h1`. A tela não tem prévia em `/previa/*`, a pedido: fica direto na aplicação.
- **A gaveta lateral** (`ClientDrawer`) é a ficha completa do cliente, na moldura da referência do usuário: o nome como título grudado no topo com Editar, abrir em tela cheia, o leque e o X na outra ponta; a linha de identidade com a foto, as etiquetas, o contato e os botões de WhatsApp, e-mail e ligar; os números da relação em quatro azulejos com fio, cada um com o rótulo em cima e o valor embaixo; e abaixo o miolo. No celular a `Dialog` troca a lateral pela bandeja, como toda camada da casa, e os botões de contato descem para a linha de baixo, porque na linha da foto roubavam a largura do nome.
- **O miolo é o mesmo do cartão de perfil** (`ClientSections`): sobre, contato, etiquetas, orçamentos vinculados e projetos. Vive num componente só porque duas telas mostram a mesma coisa com molduras diferentes, a janela que o painel abre e a gaveta da listagem; o `ClientProfile` virou a moldura de perfil em volta dele, e as etiquetas e os botões de contato saíram para funções compartilhadas no mesmo arquivo.
- **A ficha completa é buscada ao abrir**, por `loadClientAction`, e não mandada junto da listagem: ela tem anotações, etiquetas, orçamentos e projetos, e vinte e quatro delas por página encheriam a carga com o que a grade nem desenha. Enquanto vem, o cabeçalho já mostra o que o cartão sabia, então a gaveta nunca abre vazia, e o miolo dá lugar ao giro. Hoje a action lê da prévia; quando a tabela nascer, o corpo vira consulta com a RLS valendo e a assinatura continua a mesma.
- **A página tem 24 cartões**, quatro linhas cheias na largura de trabalho, que é onde a lista ainda se varre com o olho sem virar rolagem sem fim. O número é múltiplo de 2, 3, 4 e 6, as colunas que a grade forma nas larguras da casa, então a última linha raramente fica pela metade. A barra de paginação só aparece passando disso: com menos, seria uma faixa sem função no pé da tela.
- A base de exemplo é `list-preview.ts`, com os seis do painel mais vinte, fechando os 26 que o painel diz ter, o bastante para a paginação mostrar duas páginas: **o domínio não existe no banco**, e quando existir muda só a origem.

### Primeiros passos

- Configuração inicial do time **não tem rota própria**: é um modal sobre o painel, renderizado pelo próprio `/dashboard` quando `getOnboardingGate()` diz que falta configurar. Assim o primeiro acesso já cai no produto, com a configuração por cima, em vez de mandar a pessoa para outra página.
- Camada com `position: fixed` cobrindo tudo, véu em `color-mix(--color-bg-grouped 62%)` com `backdrop-filter`, e por cima o painel de vidro do cartão de login no mobile (blur com saturação, borda, sombra), com tinta leve porque sobre fundo liso o vidro puro sumiria e sobraria a borda. O véu segue o tema em vez de usar `--color-scrim` preto, senão o tema claro escureceria inteiro. Alinhamento é `align-content: safe center`, que centraliza quando cabe e vira topo quando o conteúdo passa da tela, sem cortar a primeira linha.
- O indicador de etapa é um anel no canto do cabeçalho, no lugar da marca: um arco por etapa no mesmo círculo (`dasharray` para o tamanho do arco, `dashoffset` para a posição), preenchido até a etapa atual, com o número no centro e a leitura completa em texto oculto. A troca é estado do cliente com `key` por etapa, então cada bloco monta de novo e roda `stepEnter` (opacidade e 8px de deslize), no mesmo espírito do `authEnter`.
- Etapa 1: identidade, nome, site e área de atuação. A identidade é banner mais logo, os dois enviáveis por `react-dropzone` (PNG, JPG ou WEBP até 2 MB), no formato capa de perfil: banner em 4:1 com canto squircle e a logo em círculo puro (sem `corner-shape`, pela regra dos cantos) montada sobre a borda de baixo, à esquerda. Vazio, cada alvo mostra ícone de nuvem e a dimensão ideal (1200 × 300 e 512 × 512); preenchido, a imagem ocupa o alvo inteiro.
- O endereço público (`slug`) saiu da tela: nasce do nome no servidor por `slugFromName`, que garante o mínimo de 3 caracteres mesmo com nome curto ou só de símbolo. Nome repetido não vira erro de quem preencheu: `saveTeam` tenta sufixo numérico e, por último, um aleatório, porque a colisão pode ser corrida entre dois cadastros simultâneos.
- "Site" é o endereço do próprio cliente, opcional, com `https://` como `FieldAffix` fixo em `data-tone="muted"`, o mesmo lugar do `R$` e do `%` mas em peso normal e cor terciária, porque prefixo de endereço é contexto e não pode competir com o que foi digitado. O campo guarda só o domínio, colar a URL inteira tira o protocolo repetido e `normalizeWebsite` recoloca antes do zod validar.
- Os alvos de envio centralizam o conteúdo numa camada absoluta própria, e não pelo grid do alvo: o `input` de arquivo do `react-dropzone` é filho real e entraria na conta do grid, empurrando o ícone para baixo do centro.
- Logo e banner vivem no mesmo bucket, separados pelo prefixo do arquivo (`<id da organização>/logo-…` e `…/banner-…`), então a policy de storage continua sendo uma só, presa à pasta do time.
- Área de atuação lista só o que o produto atende hoje, desenvolvimento e design, em seis opções. O `Select` abre com `placement="auto"`, então perto da base da tela a lista sobe em vez de vazar.
- A logo sobe por URL assinada: a action devolve `path` e `token`, o navegador manda o arquivo direto para o Storage e uma segunda action grava a URL pública e apaga o arquivo anterior. Passar a imagem por dentro da Server Action esbarraria no limite de corpo e ocuparia o processo do servidor.
- Etapa 2: convite com e-mail e nome, só isso. O convite entra sempre como membro e quem quiser dar mais acesso troca o papel na lista de baixo, o que tira um campo da tela sem tirar capacidade. Convite pendente mostra a etiqueta "Pendente" e aceita troca de papel (ganhou policy de update). Proprietário não é papel travado: quem for promovido a proprietário continua com o select livre, e a transferência é promover a outra pessoa e depois baixar o próprio papel. O único bloqueio é o último proprietário do time, que fica com o select desabilitado porque o `protect_last_owner` recusaria a troca de qualquer jeito.
- Remover e cancelar convite vivem dentro do dropdown de papel, depois de um divisor, e não num × ao lado. Foi o que tirou da linha o botão solto e o espaço reservado que ele exigia para os selects não desalinharem.
- O convite grava a linha, gera o token (hash no banco) e dispara o e-mail pelo Resend com link para `/convite/<token>`, que chama `accept_invite`. Falha de entrega não derruba a ação: o convite continua pendente e pode ser refeito.
- Quem entrou por convite não passa pelo fluxo: `getOnboardingGate` só pede configuração de quem é owner ou admin.
- **`insert ... returning` passa pela policy de select.** Criar o time quebrava com 42501 por isso: a associação em `organization_members` que torna a linha visível nasce no trigger AFTER INSERT, que roda depois do RETURNING ser materializado. A policy de select da organização passou a aceitar também `created_by = auth.uid()`, o que protege qualquer cliente que fale direto com a API, e o serviço gera o id e lê a linha num passo separado. Vale para toda tabela cuja visibilidade dependa de algo criado por trigger no mesmo insert.
- Etapa 3, o plano: o painel abre para 66rem, o cabeçalho padrão sai de cena e a etapa traz o próprio (logo, título, subtítulo e o alternador Mensal ou Anual com o desconto). Quatro bolas de cor sangram pelas quatro quinas, duas de cada lado, por gradiente radial em vez de filtro de desfoque: mesmo resultado sem custo de composição numa camada do tamanho do painel. Quem recorta é o painel, que ganha `overflow` escondido só nessa etapa.
- No desktop são três colunas: o plano atual fica raso, sem caixa, e as opções de subida ganham superfície e borda, cada uma com o próprio botão. O recuo é igual nos três para o conteúdo alinhar e o vão entre eles não pesar de um lado.
- No mobile viram três linhas selecionáveis (`radiogroup`), com nome e descrição à esquerda e preço à direita, e um único botão embaixo agindo sobre a linha escolhida. É troca de árvore por `useMediaQuery`, e não CSS: o cartão tem botão dentro e a linha inteira é um botão, então os dois não podem existir ao mesmo tempo no DOM.
- O catálogo (nome, descrição, preço em centavos por ciclo e recursos) fica em `features/billing/plans.ts`, longe da tela. Preço fechado não mostra centavo, porque "R$97" lê melhor que "R$97,00" numa tabela de planos.
- A etapa lê o plano em vigor e o teste gratuito do banco, por `getOnboardingGate`, e não de constante no código. Enquanto o time ainda não existe (ele nasce na etapa 1), o que chega é o catálogo com o gratuito em vigor, que é a verdade de quem acabou de se cadastrar.
- Plano gratuito conclui a configuração direto. Plano pago troca a etapa pelo checkout **no mesmo painel**, sem modal novo e sem sair para o Stripe: duas colunas dentro dos 66rem que já estavam abertos, resumo do pedido à esquerda e os campos de cartão à direita, com o mesmo gradiente das quinas atrás. Confirmado o cartão, `complete_onboarding` marca `organizations.onboarding_completed_at` e a pessoa vai para o painel.
- O resumo mostra "Total hoje R$ 0,00" e a data da primeira cobrança quando há teste gratuito, calculada no cliente porque a assinatura só nasce depois do cartão. Esse bloco só existe após um clique, então não há renderização no servidor para divergir da hidratação.
- A linha "7 dias grátis para testar" ocupa espaço reservado nos três cartões quando algum plano tem teste, senão o botão do Pro desceria uma linha sozinho e a fileira ficaria torta.

### Plano e assinatura

- `/configuracoes/plano` mostra o plano em vigor com etiqueta de situação, ciclo, fim do teste, próxima cobrança e valor; troca de plano com o alternador de ciclo; forma de pagamento com bandeira e quatro últimos dígitos; e a lista de faturas vinda do Stripe.
- Cancelar é em dois passos no próprio botão, sem modal: `Dialog` é stub, e o único modal real do projeto é o `.overlay` dos primeiros passos. O texto diz o que acontece de verdade: a assinatura fica ativa até o fim do período já pago, então cancelar não tira o acesso na hora.
- Trocar o cartão abre o mesmo formulário de cartão em modo `setup`, no lugar da linha do cartão atual. O novo cartão vira o padrão do cliente e da assinatura no mesmo passo.

### Login

- Telas de autenticação são sempre escuras, por decisão de produto. O proxy manda o caminho no header `x-pathname` e o layout raiz põe `data-theme="dark"` no `html` quando `isAuthPath()` (`lib/auth-paths.ts`, a mesma lista que o proxy usa) bate, no servidor, sem flash e sem mexer no cookie de tema. Tentativa descartada: `color-scheme: dark` só no shell não funciona, porque o Chrome resolve o `light-dark()` das variáveis no `:root`, onde elas são declaradas, e não no elemento que as usa. Foi isso também que tirou o quadrado claro que aparecia no fim da tela no mobile: era o fundo do tema claro aparecendo abaixo do conteúdo.
- Rota `/login` no grupo `(auth)`. O `layout.tsx` do grupo envolve toda tela de autenticação no `AuthCard` (`components/layout/auth-card/`), que recebe o `authHero` de `features/auth/banners.ts`: imagem, chamada curta e descrição. A página só compõe `LoginForm` de `features/auth/components/`.
- Fundo animado nos dois layouts, sem imagem nem carrossel (decisão de 2026-08-29): `GradientBlinds` (`components/ui/gradient-blinds/`, WebGL via `ogl`, portado do React Bits), em preto e branco (`--color-bg`, `--sys-gray-2`, `--color-label`) com as cores em tokens no `authHero` de `features/auth/banners.ts`, resolvidas em runtime por um `span` sonda porque o shader precisa de RGB. `mixBlendMode="normal"`: com `lighten` o `backdrop-filter` do cartão mobile não enxergava o canvas e o vidro sumia; sobre fundo preto os dois modos desenham igual. Holofote largo (`spotlightRadius` 2.2) para o gradiente aparecer inteiro sem ponteiro, e órbita automática lenta quando não há movimento por 2,5s (no celular nunca há mouse). Para em `prefers-reduced-motion` (desenha um quadro e fica) e com a aba oculta; `dpr` limitado a 1.5 para não pesar no celular. No desktop preenche a moldura da direita; no mobile é a própria tela, fixo atrás do cartão. O `Carousel` continua em `components/ui` para outro uso.
- Desktop (a partir de 64rem): duas colunas em `3fr 2fr` (60/40), com o conteúdo do login centralizado na coluna maior (`max-width: 32rem`, margem automática). Logo e título centralizados. A coluna direita é a moldura de raio `3xl` com margem 16, o `GradientBlinds` preenchendo por grid (`grid-area: 1 / 1` em todos os filhos, sem `position` no componente para não brigar com o Emotion), um degradê de `--color-scrim` subindo da base por `::after` e, sobre ele, o texto do hero: rótulo "Specular" em Geist Mono, título `title1` em `medium` e descrição `subheadline`, os dois com `--tracking-tight` (-0.02em) como o título da página, tudo em `--color-on-accent`.
- Mobile: o `GradientBlinds` é fundo fixo da tela, o hero fica oculto e o logo fica sozinho no topo, em branco (`--color-brand` trocado por `--color-on-accent` só nesse breakpoint) sobre um degradê fixo de `--color-scrim` que desce do topo até 32% e volta a partir de 55% até a base, para o logo e o cartão assentarem em fundo escuro em qualquer imagem. O formulário fica num cartão de vidro (sem divisor, título só para leitor de tela, respiros de 16): fundo `--color-bg` a 15% com `backdrop-filter: blur(12px)`, translúcido de verdade, deixando as persianas do fundo aparecerem desfocadas atrás dos campos, borda `--color-border`, raio `xl` (24; 40 ficava pesado no Safari, que arredonda sem superelipse), padding 20 e margem 16 das bordas da tela, assentado na base. Substituiu a fusão por gradiente, que dependia da altura da tela para não cair em cima de rótulo. Os botões de opção sobem para a altura `lg` (52px) só no mobile, por CSS sobre `[data-size]`, porque `size` é prop e trocar por media query em JS causaria salto na hidratação. No mobile, botões e campos do login usam raio fixo `--radius-lg` (20): o Safari não tem `corner-shape`, e raio acima de metade da altura (28 em 52, 24 em 44) vira pílula pura lá, o que no iPhone parecia `9999px`. A tela é `100dvh` com `overflow: hidden` em todo tamanho, e o layout raiz põe `data-scroll="locked"` no `html` nas rotas de auth: `globals.css` trava `html` e `body` (`overflow: hidden`, `overscroll-behavior: none`, `body` fixo), o que mata a rolagem elástica do iOS; a lateral é 12 fora e 16 dentro do cartão; o logo fica centrado no espaço que sobra acima do cartão (`grid-template-rows: 1fr auto`).
- Entrada suave em toda troca de tela: `.root` e `.confirmation` de `auth-form.module.css`, mais o `.root` do MFA, rodam a animação `authEnter` (opacidade e 6px de deslize vindo de cima) em `--duration-base` com `--ease-standard`. Como cada rota do grupo monta um elemento novo dentro do `AuthCard` que persiste, a mesma animação cobre navegação entre páginas, troca de etapa no mobile (o bloco que aparece anima) e troca de estado (formulário para "Confirme seu e-mail"). O deslize vem de cima porque contêiner de rolagem não estende a área rolável para trás: descer 6px poderia piscar barra de rolagem no `.content`. `prefers-reduced-motion` já é tratado no `reset.css`, que zera duração de animação e transição.
- Mobile em duas etapas por `data-step` e CSS, sem trocar a árvore: primeiro uma coluna com "Entrar com e-mail e senha" e "Continuar com" Google e GitHub, todos em `outline`; escolhendo e-mail, a coluna some, os campos aparecem e o rodapé "Criar agora" (que já estava na primeira etapa) dá lugar ao texto "Mude a forma de login, voltar", com "voltar" como botão em forma de link. No desktop tudo aparece de uma vez e os elementos só de mobile ficam `display: none` (via invólucro `.mobileOnly`, porque classe de módulo perde do Emotion no próprio botão).
- Opções de entrada (e-mail e provedores) em `size="md"` (44px) com o raio automático de controle, 24, o mesmo do `FieldShell`; o "Entrar" é `size="lg"` (52px) para pesar mais que as opções, por pedido do usuário. Voltar é texto, não botão.
- Provedores: cada botão é um `<form>` com `signInWithOAuth.bind(null, provedor, next)`, então funciona sem JS e o redirecionamento é do servidor. Ícones pelo `BrandIcon` (`components/ui/brand-icon/`): máscara CSS sobre o SVG de `public/brands/<nome>.svg` pintada com `currentColor`, a mesma técnica do `Logo`, então a marca segue a cor do botão nos dois temas. Com `color`, o SVG entra como imagem (`next/image`, sem otimização por ser SVG) na cor original: é o caso do Google, que tem quatro cores. GitHub e Apple são pretos no arquivo, e preto no tema escuro some, então ficam em `currentColor`, que é a versão oficial das duas marcas para fundo escuro. Nunca ícone de marca de outra fonte.
- O divisor "Faça login com e-mail" usa `--color-separator` no texto, a mesma cor das linhas, e sobe 12px para ficar colado à grade de provedores.
- Turnstile com o widget em modo **Invisible** no painel da Cloudflare (decisão de 2026-08-29): nunca desenha nada, então o container fica fora do fluxo (`position: absolute`, 0 por 0), sem cobrar nem o `gap` do grid. O `appearance: "interaction-only"` e o `size: "flexible"` ficam no código como rede de segurança caso o painel volte para Managed. Em desenvolvimento a chave de teste é a invisível (`1x00000000000000000000BB`), que espelha o modo do painel. O botão "Entrar" nasce desabilitado e só libera quando o widget devolve o token (`onVerify`), voltando a travar se expirar. Em desenvolvimento o `.env` usa as chaves de teste da Cloudflare (`1x000...AA`), que sempre passam; as reais ficam comentadas no mesmo arquivo para produção.
- Sem scroll no desktop: `.shell` tem `height: 100dvh` e `overflow: hidden`, e a coluna de conteúdo rola por dentro só se a altura não couber. No mobile o carrossel é `position: fixed`, então o fundo não acompanha a rolagem do cartão.
- Logotipo em 32px e título em `title1`, por pedido de presença. `devIndicators: false` no `next.config.ts` tira o indicador de desenvolvimento do Next, que atrapalhava a leitura da tela.
- Senha: `signInWithPassword` via `useActionState`. Rate limit `auth` por IP, Turnstile quando `TURNSTILE_SECRET_KEY` e `NEXT_PUBLIC_TURNSTILE_SITE_KEY` existem (`hasTurnstile()` em `lib/env.ts`; sem as chaves o widget não monta e a verificação não roda), zod em `signInSchema`, erro genérico "E-mail ou senha incorretos" para não vazar se o e-mail existe. Sucesso redireciona para `next` validado por `nextPathSchema`.
- "Relembrar senha" é comportamento, não enfeite: a action grava o cookie `sp-remember` (`1` ou `0`) e passa `remember` para `createClient`. Com `0`, `scopeToSession` em `lib/supabase/cookies.ts` tira `maxAge` e `expires` dos cookies do Supabase, no servidor e no proxy, então a sessão morre ao fechar o navegador. Com `1` (padrão), fica um ano.
- Erro nas telas de auth não é texto solto no formulário: vai para o `Toast`, pelo hook `useAuthToast` (`features/auth/components/use-auth-toast.ts`). Ele recebe título, mensagem e uma marca de identidade; a marca é o objeto de estado do `useActionState`, que muda a cada envio, então o mesmo erro repetido dispara o toast de novo em vez de passar despercebido. Cobre as três origens: erro da action, aviso de chegada (`?erro=` mapeado em `messages.ts` e o `#error` do fragmento) e Turnstile indisponível. Como o tom é `danger`, o toast fica na tela até a pessoa dispensar.
- `/auth/callback` entrou em `openPrefixes` no proxy. Antes, sem sessão, o callback era redirecionado para `/login` antes de trocar o código e o OAuth nunca fechava.
- Criar conta (`/cadastro`): mesmo `AuthCard` e mesmas duas etapas do mobile, com `SignUpForm` (`features/auth/components/signup-form.tsx`). O CSS é compartilhado em `auth-form.module.css` e os botões de provedor saíram para `OAuthButtons`, usados pelos dois formulários; nada duplicado. Campos: nome (vira `full_name` nos metadados, que a trigger de `profiles` lê), e-mail e senha com piso de 8 no zod (`signUpSchema`). A action `signUpWithPassword` repete o contrato do login (rate limit, Turnstile, erros amigáveis por `error.code`: conta existente, senha fraca) e chama `supabase.auth.signUp` com `emailRedirectTo` no callback. Com confirmação de e-mail ligada não há sessão na resposta: o formulário troca para o estado "Confirme seu e-mail" com o endereço enviado; se a confirmação estiver desligada, redireciona direto para `next`. Consentimento de Termos e Privacidade em texto com links abaixo do botão.
- Cadastro com e-mail já usado é bloqueado com "Já existe uma conta com esse e-mail" e link para o login. Com a proteção de enumeração do Supabase (confirmação ligada), o `signUp` de e-mail já confirmado devolve 200 com um usuário falso de `identities` vazio; é esse o sinal detectado (`data.user.identities.length === 0`, comportamento do `sanitizeUser` do gotrue). Para conta existente ainda NÃO confirmada o Supabase reenvia a confirmação e devolve o usuário real, então a tela cai no estado "Confirme seu e-mail", que é o desejado. Mostrar que a conta existe reabre a enumeração pelo formulário; decisão de produto consciente em troca de UX clara.
- Um usuário, várias formas de login: o Supabase vincula automaticamente identidades OAuth com o mesmo e-mail verificado ao mesmo usuário (padrão do gotrue, sem toggle para desligar). Google e depois GitHub com o mesmo e-mail viram UM usuário com duas identities; `profiles` não duplica porque a trigger é por id. Exceções que geram segunda conta: e-mail não verificado no provedor e e-mails diferentes entre provedores (GitHub com e-mail primário privado noreply). Conta nascida por OAuth ganha senha pelo fluxo de recuperar senha, unificando também o login por e-mail.
- Rate limit de auth em três camadas (`lib/security/rate-limit.ts` + `withinAuthLimit`/`withinEmailLimit` nas actions): por operação e IP (`auth`, 10/min), total por IP somando todas as operações de auth (`authTotal`, 30/min) e por e-mail alvo (`authEmail`, 5 por 15 min) nas ações que disparam e-mail (cadastro, reenvio, recuperação), onde limitar pelo alvo é o objetivo. No LOGIN por senha a chave é IP + e-mail e só FALHA consome a janela: `peekRateLimit` (leitura, sem consumir) barra antes de tentar e `checkRateLimit` registra só depois de senha errada. A revisão adversarial derrubou a primeira versão (chave só por e-mail, consumindo sucesso): 5 logins corretos em 15 min travavam o 6º, e qualquer pessoa com o e-mail da vítima travava o login dela com 5 POSTs por janela.
- Recuperar senha (`/recuperar-senha`): e-mail + Turnstile invisível, `resetPasswordForEmail` e estado de sucesso SEMPRE, inclusive quando o Supabase devolve erro (só loga no servidor). O `/recover` devolve 200 vazio para e-mail inexistente e o cooldown de reenvio (`over_email_send_rate_limit`) só existe para contas reais, então qualquer mensagem diferente viraria oráculo de existência; a revisão adversarial pegou exatamente isso na primeira versão. O e-mail usa o template recovery com token_hash, que leva a `/confirmar-email?type=recovery&next=/redefinir-senha`.
- Redefinir senha (`/redefinir-senha`): exige a sessão criada pela verificação do link; sem sessão mostra "O link não está mais ativo" com atalho para pedir outro. `updateUser({password})` exige aal2 quando o usuário tem TOTP (`insufficient_aal` no gotrue) e a sessão de recovery nasce aal1; o gate `mfaPending` do proxy resolve mandando para `/mfa` com `next=/redefinir-senha` preservado (única rota de auth cujo pathname sobrevive como next nos gates de MFA; `/confirmar-email` volta para `/dashboard` porque o `withNext` zera a query e o token_hash se perderia no caminho), o step-up eleva a aal2 e a troca acontece. Usuário logado pode acessar `/redefinir-senha` direto para trocar a senha; um `insufficient_aal` inesperado redireciona para `/mfa`.
- E-mails de autenticação com a nossa marca: 13 templates em `db/supabase/templates/`, os 6 de ação (confirmação, convite, magic link, redefinição, troca de e-mail e reautenticação) e os 7 avisos de segurança do painel (senha, e-mail e telefone alterados, forma de login conectada e removida, fator MFA adicionado e removido), todos gerados de um esqueleto único. Visual minimalista por decisão de 2026-08-30: fundo branco sem cartão, só o ícone da marca no topo à esquerda, título e texto soltos, botão preto pequeno só quando há ação, rodapé com linha fina, nenhum texto com ponto final. O ícone é `public/logotipo/specular-icon-email.png` (160px exibido a 40px): o ícone preto dentro de um chip branco com borda, tudo no próprio PNG, porque o modo escuro dos clientes de e-mail inverte fundo e texto sem tocar nas imagens e um ícone preto solto desaparecia no fundo escurecido. O chip sobrevive à inversão e fica legível nos dois temas. HTML de e-mail com estilos inline e tabela, logo em PNG (gerado do SVG, e-mail não renderiza SVG). `config.toml` aponta os 6 de ação para o ambiente local; no hospedado é colar tudo no painel e ligar o SMTP do Resend (tabela de slots e assuntos em `setup.md`). Cores literais aqui são exceção consciente: e-mail não tem tema nem tokens.
- Nenhum template de link usa `{{ .ConfirmationURL }}`: esse endereço vai direto ao `/verify` do Supabase e o token é de uso único, então scanner de e-mail corporativo consumia o link antes do clique e a pessoa via `otp_expired` na primeira tentativa. Os links apontam para `/confirmar-email?token_hash={{ .TokenHash }}&type=...` (página no grupo `(auth)`, mesma lista de `publicAuthPaths`, então tema escuro e acesso sem sessão), que mostra um cartão com título por tipo de link e só chama `verifyOtp` no clique do botão, via `confirmEmailWithToken` (server action com rate limit e zod em `token_hash` e `type`). Os estados de sucesso e de link incompleto (e o "Confirme seu e-mail" do cadastro) usam os ícones 3D de `public/3d-icons` (check e error) via `next/image` a 4rem de altura. Scanner faz GET e não consome nada. No sucesso a action redireciona para `next` e o proxy assume (primeiro login cai no `/mfa` pelo gate `mfaMissing`); o proxy não expulsa usuário logado do `/confirmar-email`, porque a troca de e-mail confirma com sessão ativa.
- A tela de confirmação do cadastro tem "Reenviar e-mail" (`resendConfirmation`, com rate limit e trava de 30s no botão), e o `/login` lê o fragmento `#error` que o Supabase devolve quando um link antigo falha e explica em português (link expirado ou usado), limpando o hash da URL.
- MFA (`/mfa`): uma rota, dois estados, tudo solto no AuthCard sem Surface (redesenho de 2026-08-30, seguindo o padrão das outras telas de auth). Com fator TOTP verificado, `MfaVerify`; sem fator, `MfaEnroll`: três ícones de autenticadores sobrepostos (Google Authenticator, Twilio e Microsoft Authenticator, este com o círculo preenchido no azul `#0078d7` do próprio logo, cor literal como exceção consciente de marca), título e descrição centrados, QR cru numa caixa branca sem padding e sem raio (decisão de 2026-08-31: limpo e funcional, a margem interna do próprio SVG basta), chave manual em UMA linha num box tracejado (`text-overflow: ellipsis`, fonte caption) com o "Copiar" dentro do box à direita (toast ao copiar) e `CodeInput` com `fullWidth`. O box é flex com padding de `--space-1` e a borda nele, não no `code`: assim o recuo do botão é o mesmo 4px em cima, embaixo e na direita, e a altura sai do próprio botão, que cresce para o alvo de toque no celular sem estourar a caixa. Não há botão de verificar: completar os 6 dígitos verifica sozinho nas duas telas, com "Verificando o código" durante a espera. A saída é "Sair da conta" em texto. Cantos pelo sistema da casa (`squircle()` no QR, na chave e nas caixas do código). No mobile a tela fica compacta: avatares e a linha de dica somem, o QR encolhe para 8.5rem e o gap cai para `--space-4`.
- O QR é um `<img>` com o data URL do Supabase direto no `src`: o `qr_code` vem como `data:image/svg+xml;utf-8,<svg ...>` sem encode, com aspas duplas dentro, que quebravam o `url("...")` de um `background-image` e o QR nunca renderizava (o bug só apareceu quando o enroll foi ligado no painel; o E2E anterior parava no skeleton). `next/image` não aceita data URL, então é `img` puro com o lint desativado na linha.
- `CodeInput` ganhou `fullWidth`: as caixas crescem por `flex: 1 1 auto` mantendo a base (3rem por 4rem no modo solto; raio lg via `squircle()`, dígito em `--text-title-2`). No `fullWidth` o gap cai para `--space-1` (4px) e as caixas viram QUADRADOS por `aspect-ratio: 1` com altura automática. Com `flex-basis: 0` e `width: auto` cada input voltava à largura intrínseca (~11rem) no cálculo de max-content, a trilha do grid estourava para ~70rem e o conteúdo inteiro saía da vista com scroll horizontal.
- O `.root` das telas de MFA usa `grid-template-columns: minmax(0, 1fr)`: sem isso a trilha implícita do grid cresce pelo max-content dos filhos (a chave em linha única, por exemplo) e a tela inteira ganha scroll lateral no mobile, que foi exatamente o bug reportado no iPhone.
- Confirmação de e-mail continua na aba original: `confirmEmailWithToken` para `type` signup/email não redireciona mais, devolve `done` e o cartão mostra "E-mail confirmado, volte para a outra aba" (com "Continuar por aqui" para quem abriu o link em outro aparelho). Ao confirmar, o cartão emite `confirmed` num `BroadcastChannel("sp-auth")`; a aba do cadastro (estado "Confirme seu e-mail") escuta o canal E faz polling da action `sessionEstablished` a cada 5s (os cookies são do navegador inteiro), e segue sozinha para `next` quando a sessão aparece. Os demais tipos (recovery, invite, magiclink, email_change) continuam seguindo na própria aba, porque o fluxo continua nela por natureza.
- As duas telas de MFA têm prévia na vitrine `/componentes` (grupo "Telas de autenticação"): `MfaEnroll` aceita `preview` com QR e chave falsos e pula o enroll no servidor, então dá para ver o layout em localhost sem sessão; a verificação da prévia falha de propósito. O fluxo real continua exigindo login em `/mfa`.
- `enrollTotp` passa `issuer: siteConfig.name`, então o autenticador salva a conta como "Specular" e não como o host do Site URL do projeto, que é o padrão do Supabase (a URI virava `otpauth://totp/app.specular.com.br:email`). O rótulo é gravado no momento do cadastro: quem já tinha cadastrado antes continua vendo o nome antigo no app até refazer o cadastro do fator.
- `enrollTotp` apaga os fatores TOTP não verificados antes de criar um novo, senão cada visita à tela acumularia um fator pendente; o componente ainda guarda um ref para não repetir a chamada no modo estrito do React.
- Cadastro convidado, não obrigatório (decisão de 2026-09-02): o `updateSession` devolve `mfaMissing` (aal1 sem próximo nível, ou seja, usuário sem nenhum fator verificado) e o proxy manda para `/mfa` na primeira rota protegida, mas a tela tem "Pular por agora". A `skipMfaEnrollment` grava o cookie `sp-mfa-skip` (httpOnly, 30 dias, contrato em `lib/mfa.ts`) e devolve a pessoa para o `next`; enquanto o cookie valer, o proxy para de convidar. Vencido, a tela volta a aparecer uma vez.
- **Step-up não se pula.** O cookie só silencia o convite de cadastrar: `mfaPending` (aal1 com aal2 disponível, ou seja, quem tem autenticador e ainda não digitou o código nesta sessão) continua barrando toda rota protegida sem olhar para ele, e a própria action recusa pular quando o nível seguinte é aal2. Quem pulou volta a cadastrar entrando em `/mfa` direto: sem nenhum fator a rota não devolve mais ao painel, justamente para ser esse caminho de volta.
- O conteúdo do `AuthCard` rola por dentro (`overflow-y: auto` no `.content`), porque o cartão do MFA é mais alto que a tela do celular e a página em si continua travada.
- `PasswordInput` (`components/ui/password-input/`): `Input` com `type` alternado e botão de olho na ponta, `aria-pressed` e rótulo que muda. O ícone de erro do `Field` entra antes do olho, porque o `Field` injeta `iconEnd` e o componente concatena em vez de substituir.
- O formulário não mostra asterisco: `required` vai direto no controle, e o `Field` só injeta `required` quando a prop dele é verdadeira (antes ele sobrescrevia com `false` o que o controle declarava).
- O `matcher` do proxy não usa a cláusula `missing` do exemplo da doc do Next. Ela isenta requisição de prefetch, e isenção do proxy significa rota protegida respondendo 200 sem sessão e sem CSP para quem manda `purpose: prefetch` na mão. O gate roda em toda requisição.
- Arquivo estático novo em `public/` precisa entrar no `matcher` do proxy (`logotipo`, `banners`, `bg`, `brands`, `3d-icons`). Sem isso o proxy trata a imagem como página protegida, responde redirect para `/login`, e o otimizador do `next/image`, que busca o arquivo por HTTP, recebe o redirect no lugar do PNG: a imagem quebra sem erro no console.
